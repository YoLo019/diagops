"""RCAEval RE2 的本地留置（locally held-out）准备与标签输入隔离工具。

边界说明：本包只负责把已钉住（pin）的上游数据源重打包为不透明 runtime 包
与 evaluator-only 标签包，并以「可信 runtime 的支持输入面」为威胁模型做进程
启动隔离。它是本地留置、标签输入隔离的评估，不是第三方盲测，也不声称能防御
同一 OS 账户下的恶意进程扫描任意主机路径。数据集与标签永不复制进仓库。
"""
