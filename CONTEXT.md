# DiagOps 领域语言

## 术语

**Diagnostic authority**:
被允许发布诊断的已记录决策。它来自 Agent 推理链，确定性 RCA 不能提供或改写它；V12 默认由 Critic 给出最终发布集合及顺序。
_避免说成_：任意模型输出、伪装成 Agent 诊断的确定性 fallback

**Lead adjudication**:
V11 中把已审查候选转换为发布集合的历史阶段名，不保证额外调用 Lead 模型。V12 默认称“发布诊断”；评测中的额外 Lead 裁决则是一次真实模型操作，不能混称。
_避免说成_：默认第二次 Lead model turn、Lead 投票

**Final diagnosis decision**:
诊断 Agent 明确给出的最终发布集合、顺序、引用和停止理由。候选展示序号或生成顺序不能代替这个决定。
_避免说成_：Critic accept 的自动全集、展示 rank

**Evidence digest**:
对已提交证据做出的有界、确定性 prompt 投影。它只是上下文选择视图，永远不替代完整 Evidence ledger。
_避免说成_：完整证据集、模型自行选择的证据库

**Investigation round**:
一批并行调查任务；每路内部可以多次模型请求与工具调用，直到输出 findings。第二轮由 Critic 的补证要求触发，轮次、模型请求数与工具调用数分别计量。
_避免说成_：一轮就是一次模型请求、三路并行就是三次工具调用

**Evidence coverage**:
证据条目、类型及来源的最低覆盖条件。覆盖数量不证明相互独立，同源遥测的重复查询或派生结果仍需辨明来源关系。
_避免说成_：两条 Evidence 就是两份独立证据

**Snapshot adjudication ablation**:
使用同一份已保存的候选、证据和审查追加裁决，观察最终选择与排序的变化。它只测裁决阶段，不能冒充完整诊断链路的效果或耗时。
_避免说成_：完整 Multi-Lead 对照、等成本效果验证

**Unknown model usage**:
请求已经发送，但端点没有返回可用的 token 用量。运行预算按该请求的预留额度保守扣除，恢复后继续保留；实际账单用量仍是未知。
_避免说成_：零成本失败、预留 tokens 就是实际消费

**Manual recovery**:
在原运行配置、截止时间和剩余预算内继续被中断的执行。旧版运行记录仍可读取，无法按当前执行规则继续时创建关联的新诊断，不能在恢复时清空已用额度。
_避免说成_：恢复会重置超时、旧 run 自动升级

**Failure class**:
有证据支持、短而稳定的故障分类，用于比较和下游评分；它与面向人的 failure mechanism 解释分开。
_避免说成_：完整根因解释、自由文本机制段落
