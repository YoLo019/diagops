# DiagOps V10.1 Signal Ranking Reliability Implementation Plan

状态：`approved`（含 F17/F18 的 T6 修正案于 2026-08-01 获用户重新批准）

日期：2026-08-01

Spec：`docs/superpowers/specs/2026-08-01-diagops-v10.1-signal-ranking-reliability-design.md`

## Global Constraints

以下约束逐字来自 approved spec：

```text
生产只读、确定性、无新依赖、无可调参数、历史 Evidence 可读、首次盲测后停止调参。
不使用 case ID、task、instruction、record Ground Truth 或评分结果选择规则。
不重新评分或调参已暴露 six-case；它只保留为历史失败证据。
不引入 ML、学习排序、权重配置、feature store 或新依赖。
不改变 signal taxonomy、cause taxonomy、Projector、Runtime 状态机、数据库 schema、公共 API shape 或前端。
不为 counter reset 猜测语义；缺少可靠 metadata 时保留原 gauge 路径。
不把 production Gate 7/7 表述为现实世界 100% 准确率。
```

所有注释使用简体中文和 UTF-8，只解释职责、原因、约束、边界与风险。所有非平凡
逻辑先写失败测试。不得 commit、建/切 branch、创建/删除 worktree、push、PR 或
merge，除非用户在对应 source checkpoint 对具体动作明确授权。

## 1. Execution strategy and milestones

按依赖顺序分四个 milestone 顺序执行；不把共享合同拆给并行 implementer。
经用户在 2026-08-01 批准计划时明确指示：每个 milestone 由一个独立执行
subagent 完成（M0 内 T2 的 custodian 角色由单独只读 subagent 担任），每个
milestone 完成后换另一个独立 subagent 做冷审。任务内容、顺序、门禁与
Global Constraints 不因执行方式改变。每次冷审后在
`docs/superpowers/current.md` 的 dashboard 追加状态和证据。

| Milestone | Tasks | Exit condition |
| --- | --- | --- |
| M0 | T1–T2 | Blind tooling 已验证；custodian 冻结 40-case holdout；clean V10 baseline execution bundle 已封存且未评分 |
| M1 | T3–T5 | Counter、presence-only、basis-aware ranking 合同通过，生产 caller 无回归 |
| M2 | T6–T8 | 历史兼容、全量 key-free 和生产 7/7 通过；clean candidate source 已封存 |
| M3 | T9–T10 | Candidate execution bundle、一次揭盲 paired Gate 和最终 reconciliation 完成或诚实 blocked |

Task status：

| Task | Status | Evidence |
| --- | --- | --- |
| T1 | `complete` | 63 passed focused + 116 passed benchmarks suite + Ruff clean；M0 review A `approve_with_followups`（F13/F14 low，吸收进 T2 custodian runbook） |
| T2 | `complete` | commit `4a4b20a`;custodian ledger + 40-case holdout(SHA `aac4b89b…`,overlap 0)+ V10 baseline `run-20260801T071800886742Z`(40/40,unscored);M0 review B `approve_with_followups`(F15/F16 low) |
| T3 | `complete` | 51 passed focused;counter 合同含 F10 窗口外 reset 回归 |
| T4 | `complete` | 118 passed focused;presence-only 合同与双 adapter 投影 |
| T5 | `complete` | 107 passed focused;F17 经 T6 修正关闭,consumer 层三路径顺序合同测试绿色 |
| T6 | `complete` | 247+111 focused passed;F17/F18 关闭;历史兼容/Replay/API/生产竞争回归绿色 |
| T7 | `complete` | Ruff clean;全量 1634 passed;frontend build 通过;runtime acceptance 14/14 + privacy passed;M2 review A `approve_with_followups`(F19/F20 low) |
| T8 | `not_started` | `none` |
| T9 | `not_started` | `none` |
| T10 | `not_started` | `none` |

## 2. M0 — Build blind tooling before touching diagnosis

### T1. Exclusion-aware prepare and immutable paired Gate

覆盖：R1、R10、R12、R13。

Files：

- `tests/benchmarks/test_openrca_prepare.py`
- `tests/benchmarks/test_openrca_evaluator.py`
- `backend/benchmarks/openrca/models.py`
- `backend/benchmarks/openrca/prepare.py`
- `backend/benchmarks/openrca/evaluator.py`
- `backend/benchmarks/openrca/__main__.py`

Interfaces：

- Consumes：现有 `OpenRcaRuntimeIndex`、run manifest、fixed predictions、compatible
  summary、official report、official evaluator Git root。
- Produces：
  - `prepare_cases(..., excluded_case_ids: AbstractSet[str] = frozenset()) -> PrepareResult`
  - repeatable CLI `prepare --exclude-safe-index PATH`
  - strict `OpenRcaPairedComparison` model，字段按 spec §9.3 固定；Decimal 分数以
    string 持久化。
  - `paired_gate_run(*, safe_index: Path, baseline_run_dir: Path,
    baseline_evaluation_dir: Path, candidate_run_dir: Path,
    candidate_evaluation_dir: Path, official_evaluator_root: Path,
    output_path: Path) -> list[str]`
  - CLI `paired-gate` 与 spec §9.3 完全一致。

Steps：

1. 先写 prepare tests：多个 exclusion index 合并去重、sampling 前排除、任一
   partition 不足显式失败、非法 strict index 失败、未传参数时旧 manifest hash
   和 fixture 不变。
2. 最小修改 `prepare_cases`：只增加 `excluded_case_ids` 参数，在
   `_stratified_sample` 前过滤；CLI 负责 strict load 多个 index 并传 ID set。
3. 先写 paired Gate tests，分别证明：
   - execution bundle 与 evaluation copy predictions/manifest 不一致失败；
   - evaluation manifest 除 `artifact_checksums` 外任一字段被改写时失败；移除该唯一
     允许更新字段后的 canonical JSON 必须与 execution manifest 完全一致；
   - safe-index/prediction case set 不一致失败；
   - official `(query, answer)` canonical multiset 错配、重复计数错配失败；
   - evaluator dirty/HEAD 不符失败；
   - official score 非有限、少于/多于 40、candidate 单 run gate 失败；
   - strict、三个 compatible 字段、official Decimal `+0.05` 边界独立失败；
   - output 位于受检目录内失败；失败 comparison 仍落盘。
4. 在 evaluator 内复用 stdlib `csv`、`json`、`hashlib`、`Decimal`、`Counter` 和
   现有 Pydantic；只在 `models.py` 增加一个 strict comparison model，不建新
   service、Protocol 或通用 artifact abstraction。用单一 module constant 固定
   approved official evaluator commit
   `c1bd4af7f635171a1c31cdd567c07d698dff6abc`；不新增可变配置。
5. CLI 只接线，不复制 validation。

Focused check：

```powershell
uv run pytest tests/benchmarks/test_openrca_prepare.py tests/benchmarks/test_openrca_evaluator.py -q
uv run ruff check backend/benchmarks/openrca/models.py backend/benchmarks/openrca/prepare.py backend/benchmarks/openrca/evaluator.py backend/benchmarks/openrca/__main__.py tests/benchmarks/test_openrca_prepare.py tests/benchmarks/test_openrca_evaluator.py
git diff --check
```

Expected：现有 tests 保持绿色；新增每个门禁可独立失败；诊断、Provider、schema、
dependency 文件零 diff。

### M0 review A

检查 spec F2/F4/F5/F7/F9：official answer 与 prediction 绑定、execution bundle
不写、evaluation copy 可验证、Decimal 无舍入、旧 CLI 不变。确认代码 diff 不触及
`backend/diagnosis/`、`backend/rca/`、`backend/providers/`。

### T2. Source checkpoint, custodian holdout and frozen V10 baseline

覆盖：R1、R2、R10。

Files/artifacts：

- `docs/superpowers/current.md`
- `D:\data\OpenRCA\prepared-v10.1-blind-40\{case-manifest.json,runtime-cases.json}`
- `D:\data\OpenRCA\v10.1-holdout-runtime\<partition>\telemetry\...`
- `D:\data\OpenRCA\results-v10.1-blind-baseline-v10\<run-id>\...`
- `D:\data\OpenRCA\v10.1-holdout-ledger.json`

Interfaces：

- Consumes：T1 verified tooling；exclusion index
  `D:\data\OpenRCA\prepared-v10-deterministic\runtime-cases.json`；dataset；clean
  V10 commit `7b26024`。
- Produces：custodian selection ledger、40-case safe-index/hash、无 `record.csv` 的
  runtime telemetry root、unscored immutable baseline execution bundle。

Steps：

1. 记录 `git diff --stat`，停止并请求用户分别授权：一次 focused Git commit，使
   T1 tooling/spec/plan 有 clean source identity；创建一个 detached `7b26024`
   baseline worktree。任一动作未授权则 T2 blocked，不以 dirty tooling 生成正式
   holdout，也不创建 worktree；删除该 worktree 始终另行授权。
2. 授权后先验证官方 evaluator worktree clean、HEAD 为
   `c1bd4af7f635171a1c31cdd567c07d698dff6abc`。独立 custodian 对 dataset 全部文件的
   relative path、size、SHA-256 生成排序后的输入清单及总 SHA；实施方只接收总
   SHA，不接收清单内容。
3. Custodian 确认 output/runtime/ledger 目标均不存在，再使用 seed 101、每
   partition 10、上述 exclusion index 运行：

```powershell
uv run python -m backend.benchmarks.openrca prepare `
  --dataset-root "D:\data\OpenRCA\dataset" `
  --output "D:\data\OpenRCA\prepared-v10.1-blind-40" `
  --per-partition 10 --seed 101 `
  --exclude-safe-index "D:\data\OpenRCA\prepared-v10-deterministic\runtime-cases.json"
```

   Custodian ledger 只记录 tooling source SHA/clean 状态、命令、dataset 总 SHA、
   exclusion SHA、两个输出 SHA、每 partition count、total 40、overlap 0 和 evaluator
   commit；不得记录 selected record labels。
4. Custodian 复制四个 partition 的 `telemetry` tree 到 runtime root，不复制
   `query.csv` 或 `record.csv`；验证递归搜索 `record.csv` 为零。任一目标已存在即
   停止，不覆盖或合并旧 artifact。
5. 确认 `D:\agent\worktrees\sre-agent-v10.1-baseline` 不存在，创建 detached
   `7b26024` worktree，并确认 baseline output root 与 SQLite 文件均不存在；验证
   `status --short` 为空，不创建 branch。然后运行 baseline：

```powershell
git worktree add --detach "D:\agent\worktrees\sre-agent-v10.1-baseline" 7b26024
git -C "D:\agent\worktrees\sre-agent-v10.1-baseline" status --short

$env:DIAGOPS_DATABASE_URL = "sqlite:///D:/data/OpenRCA/v10.1-runtime-baseline.db"
Push-Location "D:\agent\worktrees\sre-agent-v10.1-baseline"
try {
  uv run python -m backend.benchmarks.openrca run `
    --dataset-root "D:\data\OpenRCA\v10.1-holdout-runtime" `
    --safe-index "D:\data\OpenRCA\prepared-v10.1-blind-40\runtime-cases.json" `
    --strategy fixed --mode deterministic `
    --output "D:\data\OpenRCA\results-v10.1-blind-baseline-v10"
} finally {
  Pop-Location
}
```

6. 记录 baseline run ID、source SHA、manifest SHA、fixed predictions SHA、summary
   SHA、40/40 execution completion 与 case set；不得运行 `evaluate`、official
   evaluator 或任何 score parser。

Focused check：

```powershell
Get-FileHash -Algorithm SHA256 "D:\data\OpenRCA\prepared-v10.1-blind-40\case-manifest.json"
Get-FileHash -Algorithm SHA256 "D:\data\OpenRCA\prepared-v10.1-blind-40\runtime-cases.json"
Get-ChildItem -LiteralPath "D:\data\OpenRCA\v10.1-holdout-runtime" -Recurse -Filter record.csv
git -C "D:\agent\worktrees\sre-agent-v10.1-baseline" status --short
```

Expected：40 selected、0 overlap、runtime root 无 record、V10 worktree clean、baseline
40/40 completed；没有 compatible/official report 和分数。

### M0 review B

独立核对 custodian ledger、case set、source/hash、runtime root 和 baseline bundle；
不打开 instruction 之外的 label/record。M0 未满足不得开始 T3。

## 3. M1 — Correct signal semantics

### T3. Conservative OpenRCA counter-like normalization

覆盖：R3、R9、R12。

Files：

- `tests/benchmarks/test_openrca_providers.py`
- `backend/benchmarks/openrca/providers.py`

Interfaces：

- Consumes：按 `(component, instance, metric_name)` 聚合的 `list[SeriesPoint]`。
- Produces：
  - `_is_counter_like_name(metric_name: str) -> bool`
  - `_normalize_counter_like(metric_name: str, points: Sequence[SeriesPoint]) -> list[SeriesPoint]`
  - 右侧 timestamp、per-second delta 的 rate points，或语义不确定时的原 points。

Steps：

1. 先写失败 tests：`total_*`、`*_total`、`*_count`、`*_counter`；等速绝对值不
   产生异常；rate 加速产生正确 onset；单调 memory gauge、不满足命名、常量、
   reset、排序后重复 timestamp、非有限输入不转换；乱序输入先排序再判定；窗口外
   reset 不得阻止窗口内合法 counter 转换。
2. 实现两个 private helpers；只用 stdlib。按 spec 写一条 `ponytail:` 中文注释：
   当前只识别 name+shape，无可靠 metric-type metadata 时不猜 reset。
3. `OpenRcaMetricProvider.collect` 先只保留 `[baseline_start, end]` relevant points，
   再调用一次 normalization，最后按 `start` 分 baseline/observation；不得让窗口外
   shape 参与 counter 判定。其他 Provider 不变。

Focused check：

```powershell
uv run pytest tests/benchmarks/test_openrca_providers.py -q
uv run ruff check backend/benchmarks/openrca/providers.py tests/benchmarks/test_openrca_providers.py
```

Expected：counter 合同绿色，原 schema/family/component tests 不变。

### T4. Presence-only segment and additive payload

覆盖：R4、R5、R7、R9。

Files：

- `tests/diagnosis/test_signal_semantics.py`
- `tests/benchmarks/test_openrca_providers.py`
- `tests/providers/test_prometheus_provider.py`
- `backend/diagnosis/signal_semantics.py`
- `backend/benchmarks/openrca/providers.py`
- `backend/providers/prometheus.py`

Interfaces：

- Consumes：现有 `detect_anomaly_segments(baseline_points, observation_points)`。
- Produces：
  - `AnomalySegment.strength_basis: Literal["relative", "presence_only"]`
  - 现有 `AnomalySegment.point_count`
  - additive payload `strength_basis` 和 `anomaly_point_count`。

Steps：

1. 先写 Signal Core tests：`abs(median)<=epsilon && MAD<=epsilon` 的非零 observation
   产生 strength 1/presence-only；持续异常保留 point count；正常 baseline 继续
   relative 0..10；空/non-finite 行为不变。
2. 最小修改 dataclass、`detect_anomaly_segments`、`_close_segment`，不新建 enum 或
   config。
3. 两个 metric adapter 同步投影字段；instant Prometheus Evidence 不伪造 segment
   字段，保持现有 onset-unavailable 语义。
4. 测试 payload 值域和历史 fixture 不要求新字段。

Focused check：

```powershell
uv run pytest tests/diagnosis/test_signal_semantics.py tests/benchmarks/test_openrca_providers.py tests/providers/test_prometheus_provider.py -q
uv run ruff check backend/diagnosis/signal_semantics.py backend/benchmarks/openrca/providers.py backend/providers/prometheus.py tests/diagnosis/test_signal_semantics.py tests/benchmarks/test_openrca_providers.py tests/providers/test_prometheus_provider.py
```

Expected：两个 range adapter 字段一致；instant fallback 与旧 payload 可读。

### T5. Basis-aware selection and Attribution

覆盖：R6、R7、R8。

Files：

- `tests/diagnosis/test_signal_semantics.py`
- `tests/diagnosis/test_coordination_review.py`
- `backend/diagnosis/signal_semantics.py`
- `backend/diagnosis/coordination_review.py`

Interfaces：

- Consumes：Evidence payload 的 optional `strength_basis`、
  `anomaly_point_count`、现有 strength/provider/signal/onset。
- Produces：private derived accessors `_strength_basis(item) -> str`、
  `_point_count(item) -> int`；family basis queues；Attribution basis cohorts；公开函数
  签名不变。

Steps：

1. 先写 selection tests：relative 内 strength；presence 内 point count；跨 basis
   只比较共同 support；family-first、limit、去重、输入反转可重复性不变。
2. 先写 Attribution tests：relative/presence/mixed/legacy cohorts；跨 basis 不使用
   strength；相同 support 使用 onset/component/reason；全 legacy 输出顺序与 V10
   fixture 相同。
3. 实现最少 private helpers 和队列/cohort merge；不修改 cluster score 公式本身，
   只限制它在 relative cohort 内使用。
4. malformed/boolean/non-finite strength、非法 point count 统一派生安全默认，不写回
   payload。

Focused check：

```powershell
uv run pytest tests/diagnosis/test_signal_semantics.py tests/diagnosis/test_coordination_review.py -q
uv run ruff check backend/diagnosis/signal_semantics.py backend/diagnosis/coordination_review.py tests/diagnosis/test_signal_semantics.py tests/diagnosis/test_coordination_review.py
```

Expected：F6 cross-basis 竞争测试绿色；公开 API 和历史 payload 不变。

### M1 review

合并运行 T3–T5 suites；冷审所有 `detect_anomaly_segments` callers、Analyzer
`_is_strong`、payload consumers。检查无 task/case/Ground Truth token、无新依赖、
无 weight/config；确认 `ponytail:` ceiling 准确。

M1 review 结果（2026-08-01，独立 subagent 冷审）：T3、T4、T5 selection 半侧、
纪律边界全部通过；发现 blocking F17（domain validator onset 重排架空 Attribution
cohort 排序，R6 Attribution 半侧未达成）与 medium F18（dependency payload 未投影
additive 字段）。两条已 accepted 并纳入 T6 修正范围（见下）；consumer 层顺序合同
测试通过前 T5 不关闭。

## 4. M2 — Compatibility, regression and production proof

### T6. Historical persistence, Replay and API compatibility

覆盖：R5、R6、R8、R11、R12。

Files：只在测试失败证明需要时修改生产 consumer；预期主要修改：

- `tests/rca/test_analyzer.py`
- `tests/runtime/test_replay.py`
- `tests/db/test_sqlite_repository.py`
- `tests/api/test_investigations_api.py`
- `tests/services/test_production_acceptance.py`
- `tests/diagnosis/test_coordination_review.py`（F17 consumer 层顺序合同测试）
- `backend/domain/agent_findings.py`（F17：`sort_ranked_values` 保留 `root_causes`
  的 attribution 排序，`candidates` 按 rank 排序不变）
- `backend/benchmarks/openrca/providers.py`（F18：`_dependency_payload` 投影
  `strength_basis`/`anomaly_point_count`）
- `tests/benchmarks/test_openrca_providers.py`（F18 payload 测试）

Interfaces：

- Consumes：新、旧和 mixed Evidence payload；SQLite/InMemory records；Replay events。
- Produces：历史缺字段的 derived legacy/default 读取证据；API additive payload；
  production presence-only 与 relative 噪声竞争回归；consumer 层（构建与持久化
  reload 两条路径）`root_causes` 顺序与 `build_root_cause_attributions` 输出一致。

Steps：

1. 增加一组最小跨层 tests：旧 payload round-trip 不写回 `legacy`；Replay 输出不
   漂移；API 仍返回旧 payload；新 additive payload 保留。
2. 增加生产竞争案例：真实零基线 process/network signal 与 relative 噪声同时
   存在，真实信号不因 numeric cross-basis strength 落后。
3. F17：先写 consumer 层失败测试——经 `CoordinationReview` 构建和经
   SQLite/InMemory reload 后，`root_causes` 顺序仍等于
   `build_root_cause_attributions` 的输出顺序（含 F6 cross-cohort 场景）；再最小
   修改 `sort_ranked_values`，移除对 `root_causes` 的 onset 重排，保留
   `candidates` 按 rank 排序。补注释说明排序所有权在 attribution 构建层。
4. F18：先写 `_dependency_payload` 投影失败测试（含退化 dependency baseline 的
   presence-only 值域），再补两字段投影，与 metric adapter 同一模式。
5. 只有测试证明 consumer 需要修改才改生产文件；不得先加 migration、normalizer
   service 或 frontend 类型。

Focused check：

```powershell
uv run pytest tests/rca/test_analyzer.py tests/runtime/test_replay.py tests/db/test_sqlite_repository.py tests/api tests/services/test_production_acceptance.py -q
uv run pytest tests/diagnosis/test_coordination_review.py tests/benchmarks/test_openrca_providers.py -q
uv run ruff check backend tests
```

Expected：历史数据、Replay、API、production artifact V1/V2 兼容；无 schema diff；
F17/F18 合同测试绿色，review 层顺序与 attribution 构建层一致。

### T7. Key-free full regression

覆盖：R3–R12。

Interfaces：

- Consumes：T3–T6 的 verified diff、focused test evidence 和现有 frontend/runtime
  acceptance entry points。
- Produces：Ruff、全 pytest、frontend build、Runtime acceptance、diff boundary 的
  source-identity-bound evidence。

```powershell
uv run ruff check .
uv run pytest -q
npm.cmd --prefix frontend run build
uv run python -m backend.services.runtime_acceptance
git diff --check
```

Expected：Ruff、全 pytest、frontend build、Runtime acceptance 14/14/privacy 全部
通过；`pyproject.toml`、`uv.lock`、DB schema/migrations、package files、公共 API 和
write Tool 零不授权变化。

### M2 review A

Cold review M1+M2 diff，按 spec O1/O2/O4/O6 检查共享 caller、历史读取、生产安全、
失败/partial 语义和多余代码。Blocking finding 修复并 focused re-review 后才能进入
source checkpoint。

### T8. Candidate source checkpoint and production Gate

覆盖：R11、R12。

Interfaces：

- Consumes：T7 全量通过的 candidate diff、holdout/baseline hashes、用户 Git 授权。
- Produces：clean candidate source SHA 和与该 SHA 绑定的 production Gate artifact；
  不产生 holdout score。

1. 记录 diff、tests、HEAD、holdout hash 和 baseline bundle hash；停止并请求用户
   授权 candidate Git commit。未授权不得运行正式 production/holdout Gate。
2. 授权后验证 clean source，运行：

```powershell
powershell -ExecutionPolicy Bypass -File scripts/run_production_gate.ps1
```

Expected：7/7；Evidence 100%；read-only 0；privacy passed；新三场景 onset
`<=60s`；总耗时 `<900s`。失败立即归档并停止，不运行 candidate holdout。

### M2 review B

核对 production artifact/source/checksum 和 scenario assertions；不得把 7/7 写成
现实准确率。通过后才进入 M3。

## 5. M3 — Freeze candidate and reveal once

### T9. Candidate execution bundle and paired evaluation

覆盖：R2、R10、R13、R14。

Artifacts：

- `D:\data\OpenRCA\results-v10.1-blind-candidate\<run-id>`
- `D:\data\OpenRCA\evaluations-v10.1\baseline\`
- `D:\data\OpenRCA\evaluations-v10.1\candidate\`
- `D:\data\OpenRCA\official-queries-v10.1\{baseline,candidate}\`
- `D:\data\OpenRCA\comparisons-v10.1\comparison.json`

Interfaces：

- Consumes：approved spec §9.2/§9.3、同一 safe-index、immutable baseline/candidate
  bundles、clean frozen official evaluator 和 T8 production pass。
- Produces：两个只评分一次的 verified evaluation copies、official reports 和唯一
  `comparison.json` pass/fail artifact。

Steps：

1. 确认 candidate output root 与 SQLite 文件均不存在；在 clean candidate source、
   独立 SQLite DB、同一 runtime root/safe-index 运行一次：

```powershell
$env:DIAGOPS_DATABASE_URL = "sqlite:///D:/data/OpenRCA/v10.1-runtime-candidate.db"
uv run python -m backend.benchmarks.openrca run `
  --dataset-root "D:\data\OpenRCA\v10.1-holdout-runtime" `
  --safe-index "D:\data\OpenRCA\prepared-v10.1-blind-40\runtime-cases.json" `
  --strategy fixed --mode deterministic `
  --output "D:\data\OpenRCA\results-v10.1-blind-candidate"
```

2. 验证 candidate 40/40、case set/hash 与 baseline 相同，封存 execution bundle。
3. 用 native PowerShell 把 baseline/candidate bundle 各复制一次到空的显式 evaluation
   directory；复制前验证目标不存在，复制后验证 manifest/prediction SHA。不得覆盖
   旧 evaluation directory。
4. 确认 official evaluator clean 且 HEAD 等于 T2 冻结的
   `c1bd4af7f635171a1c31cdd567c07d698dff6abc`。仅对 copies 运行 compatible
   evaluate 和 official query generation；query output 目标必须不存在：

```powershell
$datasetRoot = "D:\data\OpenRCA\dataset"
$officialRoot = "D:\data\OpenRCA\official-evaluator"
$evaluationRoot = "D:\data\OpenRCA\evaluations-v10.1"
$queryRoot = "D:\data\OpenRCA\official-queries-v10.1"

foreach ($side in @("baseline", "candidate")) {
  $evaluationDir = Join-Path $evaluationRoot $side
  $queryDir = Join-Path $queryRoot $side
  uv run python -m backend.benchmarks.openrca evaluate `
    --query-root $datasetRoot --run-dir $evaluationDir `
    --official-query-output $queryDir

  $predictionFiles = Get-ChildItem -LiteralPath $evaluationDir -Filter "fixed-*.csv" |
    Where-Object Name -NE "fixed-predictions.csv" | Sort-Object Name
  $queryFiles = foreach ($prediction in $predictionFiles) {
    $slug = $prediction.BaseName.Substring("fixed-".Length)
    Join-Path $queryDir "$slug-query.csv"
  }
  Push-Location $officialRoot
  try {
    python -m main.evaluate -p $predictionFiles.FullName -q $queryFiles `
      -r "$evaluationDir\official-report.csv"
  } finally {
    Pop-Location
  }
}
```
5. 先对 candidate evaluation copy 运行现有 `gate`；失败则仍生成 comparison failure
   artifact，但不声明提分。
6. 运行：

```powershell
uv run python -m backend.benchmarks.openrca paired-gate `
  --safe-index "D:\data\OpenRCA\prepared-v10.1-blind-40\runtime-cases.json" `
  --baseline-run-dir <baseline-execution-bundle> `
  --baseline-evaluation-dir "D:\data\OpenRCA\evaluations-v10.1\baseline" `
  --candidate-run-dir <candidate-execution-bundle> `
  --candidate-evaluation-dir "D:\data\OpenRCA\evaluations-v10.1\candidate" `
  --official-evaluator-root "D:\data\OpenRCA\official-evaluator" `
  --output "D:\data\OpenRCA\comparisons-v10.1\comparison.json"
```

Expected pass：candidate 单 run gate passed；official partial delta `>=0.05`；compatible
strict 不回退；component/reason/time 全部正且不回退；Evidence 100%；projection
error/fallback 0；read-only 0；tokens/cost 0；所有 identity/checksum 完整。

Copy identity rule：paired Gate 以 execution manifest SHA 作为 `pre_eval_manifest_sha`；
evaluation manifest 只允许 `evaluate_run` 更新 `artifact_checksums`。移除该字段后，
两边按 sorted-key canonical JSON 必须完全一致；fixed predictions 必须 byte-for-byte
一致。不得把 post-eval manifest SHA 当作 pre-eval identity。

Stop rule：无论 pass/fail，holdout 立即 exposed。本迭代禁止修改诊断规则或重跑来
改变结论。失败保留全部 unfavorable artifacts，R13 标为 blocked，不声称准确率
提升；任何新算法迭代使用新 holdout。

### T10. Final reconciliation

覆盖：R1–R14。

Interfaces：

- Consumes：T1–T9 focused/full/production/paired evidence、source IDs、artifact hashes
  和 plan review ledger。
- Produces：spec 唯一 traceability 最终状态、`current.md` 最终路由状态，以及诚实的
  `complete` 或 owner 明确的 `blocked` 结论。

1. 用 fresh evidence 更新 spec 唯一 traceability table：每项 `verified` 或有 owner
   的 `blocked`；review ledger 只有 regression check 通过后关闭。
2. 更新 `current.md`：source commits、holdout manifest/hash、baseline/candidate run
   IDs、production/comparison artifact hashes、真实 Gate 结果。
3. 只有 R1–R14 全 verified 且无 blocking finding，才设置 iteration/implementation
   `complete`；R13 失败则按 spec 记录 blocked，不用生产 7/7 掩盖。
4. 不 commit、push、建 PR、merge 或删除 worktree，除非用户另行授权。

Final verification：

```powershell
uv run ruff check .
uv run pytest -q
npm.cmd --prefix frontend run build
uv run python -m backend.services.runtime_acceptance
git diff --check
```

## 6. Dependency order and review gates

```text
T1 -> user Git authorization -> T2 holdout + V10 baseline
   -> T3 -> T4 -> T5 -> M1 review
   -> T6 -> T7 -> M2 review A
   -> user candidate Git authorization -> T8 production Gate -> M2 review B
   -> T9 one-time reveal -> T10 reconciliation
```

T2 的外部 owner 是 holdout custodian；两个 Git source checkpoints 的 owner 是用户。
任一 checkpoint 未授权只阻塞后续正式 artifact，不扩大授权范围。

## 7. Plan review ledger

本计划复用 approved spec §13 的 review ledger；plan cold review findings 继续编号
F10 起，记录在 spec 同一表中，不创建第二份 ledger。独立 cold review 结论为无
blocking；F10、F11 两项 high 和 F12 一项 medium 已接受并修改计划，focused
re-review 全部 closed。

## 8. Approval state

本计划于 2026-08-01 由用户批准，批准时用户同时明确指示执行策略改为"每个
milestone 一个执行 subagent + 独立 review subagent"（已记录于第 1 节，不改变
任务合同与门禁）。R1–R14 已映射到 T1–T10、focused checks 和 final gates；独立
cold review findings 已全部关闭。

2026-08-01 M1 review 发现 blocking F17 与 medium F18（记录在 spec §13 同一
ledger），修复需要改动 T5/T6 原文件清单之外的 `backend/domain/agent_findings.py`
与 `backend/benchmarks/openrca/providers.py`，属 implementation ownership 变更：
plan 退回 `review_required`，T6 范围已按 F17/F18 Resolution 修正。用户于
2026-08-01 重新批准修正后的 plan，从 T6 继续执行。spec 合同（R6 的
support-先于-onset 意图）不变，spec 保持 `approved`。
