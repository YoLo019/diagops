# DiagOps V8.1 Reliability And Provider Compatibility Implementation Plan

> **Execution workflow:** Use `iteration-flow` Full mode. Execute the three milestones below in order. Use subagents only for independent work with non-overlapping files/contracts or for the two milestone reviews. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Harden deterministic evidence, human state, SQLite integrity, sensitive-data handling, and legacy compatibility; then close the V7 live-reliability gaps, persist safe structured execution diagnostics, produce schema-v3 reliability artifacts, and run the same multi-agent RCA runtime with explicit OpenAI or DeepSeek model Providers.

**Architecture:** Preserve deterministic RCA and the existing OpenAI Agents SDK orchestration as the authority boundary. First separate simulated Providers from real incident input, centralize redaction and reference validation, enforce server-owned human transitions, migrate SQLite safely, retire superseded writers while preserving historical readers, and make API/UI loading resilient. Then align hybrid agreement and safe execution contracts, persist them atomically, pass the unchanged V7 OpenAI gate, and only afterward add one narrow DeepSeek Chat Completions adapter with strict local Pydantic validation and independent Provider certification.

**Tech Stack:** Python 3.12, FastAPI, Pydantic, SQLAlchemy/SQLite, OpenAI Agents SDK, OpenAI Python client, pytest, Ruff, React/TypeScript, Vite.

---

## Execution Rules

1. Work in an isolated `codex/` branch or worktree created at execution time.
2. Do not request, print, store, or commit an API key.
3. Complete Baseline Tasks A-E and Tasks 1-5, pass their focused checks and the canonical OpenAI V7 gate, before starting Task 6.
4. Apply one live-diagnostic fix at a time. If Task 5 reveals a new prompt, mapping, timeout, or arbitration defect not already authorized by the V8.1 spec, stop and write a focused spec amendment before changing behavior.
5. Do not lower reliability thresholds, relax evidence validation, repair malformed JSON, enable automatic Provider fallback, or add production mutation tools.
6. Commit steps are execution instructions only; do not commit unless the user authorizes git actions for the execution session.
7. At execution start, update `docs/superpowers/current.md` to `Iteration status: implementing` and `Implementation status: in_progress` before changing implementation files.
8. When a stop condition blocks further execution, update `current.md` to `Iteration status: blocked`, `Implementation status: blocked`, and record a concise non-`none` `Blocker`.
9. Immediately before final verification, update `current.md` to `Iteration status: verifying` and `Implementation status: verifying`.
10. Mark the iteration `complete` only after all required checks pass and an authorized completion commit exists. Record that commit and concise verification evidence in `current.md`. Without commit authorization, leave the iteration in `verifying` and report the pending finish action.

## Milestones And Review Gates

### Milestone 1: Baseline hardening and OpenAI stabilization

Execute Baseline Tasks A-E and Tasks 1-5. Mechanical steps remain inline; use a subagent only when work is genuinely independent with a non-overlapping file set. Then run one milestone review with `AGENT.md`, the relevant V8.1 spec and plan sections, the complete Milestone 1 diff, focused test and migration-smoke results, and the canonical OpenAI gate artifact summary. Review spec coverage, persisted-data compatibility, redaction-before-persistence, state-transition safety, concurrency, legacy-read compatibility, failure visibility, evidence integrity, and whether Task 5 permits Provider compatibility to begin. Do not start Task 6 until blocking findings are resolved and the Task 5 stop gate passes.

### Milestone 2: Provider compatibility and deterministic verification

Execute Tasks 6-9. Then run one milestone review with the same required context plus the complete Milestone 2 diff and current full verification results. Review the serialized DeepSeek request contract, strict local output validation, missing-credential attribution, Provider isolation, certification semantics, secret handling, deterministic fallback, and absence of a second Agent runtime. Do not start paid Provider certification until blocking findings are resolved.

### Milestone 3: Independent certification and finish

Execute Task 10. This milestone uses the final review and verification in Task 10 rather than dispatching an additional reviewer. Record skipped live checks and residual risk explicitly; a Provider may be `not_run` only where the approved spec permits it.

## File Map

### Baseline truthfulness and safety

- Create `backend/safety/redaction.py`: recursive value redaction, safe failure projection, and Markdown escaping shared by Providers, persistence, reports, API, and Agent projections.
- Create `backend/diagnosis/evidence_validation.py`: duplicate-ID, Provider-status, hypothesis, finding, and candidate semantic validation.
- Modify `backend/providers/registry.py`: simulation-only Provider partition, missing-capability results, Provider-result validation, and partial-result preservation.
- Modify `backend/providers/file_logs.py`: bounded reads and redacted samples without persisted source paths.
- Modify `backend/providers/file_deployments.py`: bounded records and strict service/environment/time filtering.
- Modify `backend/providers/file_service_catalog.py`: bounded static lookup with explicit service/environment scope.
- Modify `backend/providers/prometheus.py`: bounded query results and no persisted Base URL or raw remote exception.
- Modify `backend/rca/analyzer.py`: consume only validated `success|partial` supporting evidence and fall back to `unknown` when support is absent.
- Modify `backend/reports/generator.py`: escape every dynamic field and show Evidence ID plus collection status.
- Modify `backend/domain/actions.py`: additive verification result references.
- Create `backend/domain/human_transitions.py`: action and verification transition/reference rules shared by repositories.
- Modify `backend/api/investigations.py`: stable 404/409/422 behavior and summary-list endpoint.

### SQLite and compatibility lifecycle

- Create `backend/db/migrations.py`: ordered transactional SQLite migrations and post-migration schema validation.
- Modify `backend/db/session.py`: schema manifest execution, per-connection foreign keys, correct in-memory pooling, and explicit engine disposal ownership.
- Modify `backend/db/schema.py`: current schema columns/indexes required by targeted atomic child updates.
- Modify `backend/db/sqlite_repository.py`: non-destructive aggregate upsert, targeted atomic updates, stale-write protection, atomic V7 result persistence, and read-only historical ReAct/LLM access.
- Modify `backend/db/repositories.py`: lock-protected in-memory transitions and removal of retired writers while retaining historical readers.
- Modify `backend/db/serialization.py`: preserve old LLM-analysis reads without writing or deleting historical rows.
- Delete `backend/diagnosis/react_agent.py`: retired V6 runtime.
- Delete `backend/diagnosis/llm_analyst.py`: retired pseudo-LLM runtime.
- Modify `backend/diagnosis/orchestrator.py`: remove retired execution paths and preserve deterministic results for every optional Agent failure.
- Modify `backend/config/settings.py`: remove `llm` and `react` settings and environment overrides.
- Modify `backend/services/container.py`: one repository/engine lifecycle and no retired runtime construction.
- Modify `backend/api/agent_views.py`: keep historical ReAct reads; use atomic persisted V7 state.
- Modify `frontend/src/api.ts`: summary-list, independent detail, transition-reference, and compatibility types.
- Modify `frontend/src/App.tsx`: remove ReAct panel, constrain human transitions, and preserve successful sections when optional endpoints fail.

### Domain and runtime contracts

- Modify `backend/domain/multi_agent.py`: Provider, execution-step, failure, and stabilization enums; attributed run summary.
- Modify `backend/domain/agent_plan.py`: persisted execution step, attempt, failure, Provider, and model fields.
- Modify `backend/domain/agent_findings.py`: persisted review Provider/model and stabilization fields.
- Modify `backend/diagnosis/coordination_review.py`: corrected hybrid agreement and attributed review construction.
- Modify `backend/diagnosis/agents_runtime.py`: structured step/failure records and Provider/model propagation.

### Persistence, API, and frontend

- Modify `backend/db/repositories.py`: preserve new fields through in-memory repository operations.
- Modify `backend/db/sqlite_repository.py`: round-trip attributed payload fields through the validated schema-v5 repository and atomic Agent-result operation.
- Modify `backend/api/agent_views.py`: expose structured run diagnostics without parsing error text.
- Modify `frontend/src/api.ts`: additive V8.1 types.
- Modify `frontend/src/App.tsx`: compact Provider/model, execution stage, failure, and certification display.
- Modify `frontend/src/styles.css`: reuse existing compact status styling; add only selectors required by the new rows.

### Evaluation and Provider compatibility

- Create `backend/services/reliability_artifacts.py`: schema-v3 artifact models, writer, reader, redaction checks, and latest certification lookup.
- Modify `backend/services/v7_live_acceptance.py`: safe execution projection, V8.1 evaluation fields, provider-aware live configuration, schema-v3 output, deterministic/live modes.
- Create `backend/diagnosis/deepseek_model.py`: fixed official DeepSeek Chat Completions model adapter and capability profile.
- Modify `backend/config/settings.py`: explicit `openai | deepseek` selection.
- Modify `config/diagops.yaml`: backward-compatible default Provider.
- Modify `backend/services/container.py`: construct exactly one selected model Provider.
- Modify `backend/api/config.py`: expose non-secret Agent Provider implementation/certification state.
- Modify `README.md`: configuration, evaluation, safety, and certification commands.
- Modify `docs/superpowers/current.md`: real implementation, verification, completion, and blocker status.

### Tests

- Create `tests/safety/test_redaction.py`.
- Create `tests/diagnosis/test_evidence_validation.py`.
- Modify `tests/providers/test_mock_providers.py`.
- Modify `tests/providers/test_file_logs.py`.
- Modify `tests/providers/test_file_deployments.py`.
- Modify `tests/providers/test_file_service_catalog.py`.
- Modify `tests/providers/test_prometheus_provider.py`.
- Modify `tests/rca/test_analyzer.py`.
- Modify `tests/reports/test_generator.py`.
- Modify `tests/domain/test_action_models.py`.
- Modify `tests/api/test_events_api.py`.
- Modify `tests/api/test_investigations_api.py`.
- Modify `tests/db/test_sqlite_repository.py`.
- Create `tests/db/test_migrations.py`.
- Create `tests/db/test_concurrency.py`.
- Modify `tests/api/test_v6_react_trace_api.py`: retain only direct historical-row read compatibility cases.
- Modify `tests/db/test_v6_react_trace_persistence.py`: retain only direct historical-row SQLite read cases; remove writer requirements.
- Delete `tests/diagnosis/test_react_agent.py`.
- Delete `tests/diagnosis/test_llm_analyst.py`.
- Modify `tests/domain/test_multi_agent_models.py`.
- Modify `tests/domain/test_agent_models.py`.
- Modify `tests/domain/test_agent_findings.py`.
- Modify `tests/diagnosis/test_coordination_review.py`.
- Modify `tests/diagnosis/test_agents_runtime.py`.
- Modify `tests/diagnosis/test_agents_sdk_contract.py`.
- Modify `tests/db/test_v5_agentic_rca_persistence.py`.
- Modify `tests/api/test_v5_agentic_rca_api.py`.
- Modify `tests/config/test_settings.py`.
- Modify `tests/diagnosis/test_orchestrator.py`.
- Modify `tests/services/test_v7_live_acceptance.py`.
- Create `tests/services/test_reliability_artifacts.py`.
- Create `tests/diagnosis/test_deepseek_model.py`.
- Modify `tests/frontend/test_frontend_smoke.py`.

---

### Baseline Task A: Enforce Provider And Evidence Truthfulness

**Files:**
- Modify: `backend/providers/registry.py`
- Modify: `backend/providers/file_logs.py`
- Modify: `backend/providers/file_deployments.py`
- Modify: `backend/providers/file_service_catalog.py`
- Modify: `backend/providers/prometheus.py`
- Create: `backend/diagnosis/evidence_validation.py`
- Modify: `backend/rca/analyzer.py`
- Modify: `backend/diagnosis/orchestrator.py`
- Test: `tests/providers/test_mock_providers.py`
- Test: `tests/providers/test_file_logs.py`
- Test: `tests/providers/test_file_deployments.py`
- Test: `tests/providers/test_file_service_catalog.py`
- Test: `tests/providers/test_prometheus_provider.py`
- Create: `tests/diagnosis/test_evidence_validation.py`
- Modify: `tests/rca/test_analyzer.py`
- Modify: `tests/api/test_events_api.py`

- [x] **Step 1: Write failing simulation-boundary and missing-capability tests**

Add tests proving a registry built from the default settings contributes mock
evidence only to `IncidentSource.SIMULATED`. For otherwise identical manual and
webhook events, assert no mock Provider is called, each unavailable evidence
capability has a `ProviderStatus.SKIPPED` result, and RCA returns `unknown`
rather than the fixed mock `deployment_regression` result.

Use a recording mock Provider so the test proves absence of invocation, not
only absence of its final evidence:

```python
class RecordingProvider:
    provider = EvidenceProvider.LOG

    def __init__(self) -> None:
        self.calls = 0

    def collect(self, event: IncidentEvent) -> ProviderResult:
        self.calls += 1
        return ProviderResult(provider=self.provider, evidence_items=[log_evidence()])


registry = ProviderRegistry(providers=[], simulation_providers=[recording])
results = registry.collect_results(manual_event())
assert recording.calls == 0
assert any(result.status == ProviderStatus.SKIPPED for result in results)
```

Run:

```powershell
uv run pytest tests/providers/test_mock_providers.py tests/api/test_events_api.py -v
```

Expected: FAIL because current settings mix mock Providers into every source.

- [x] **Step 2: Write failing duplicate, status, and semantic-reference tests**

Create `tests/diagnosis/test_evidence_validation.py` for these exact cases:

1. Duplicate Evidence IDs across two Provider results raise
   `EvidenceContractError("duplicate_evidence_id", evidence_id)`.
2. A failed/skipped result containing evidence fails closed.
3. An Evidence item whose `provider` differs from its parent result fails.
4. Concrete hypotheses with no supporting IDs, unknown IDs, failed/skipped
   support, wrong cause semantics, duplicate rank, non-finite confidence, or
   inconsistent support/contradiction direction fail before persistence.
5. `unknown` remains valid with no supporting evidence.

Define the public validation boundary in the tests:

```python
validated = validate_investigation_evidence(provider_results)
validate_hypotheses(validated.supporting_evidence, hypotheses)
validate_agent_semantics(validated.supporting_evidence, findings, candidates)
```

Use one explicit cause-support map shared by deterministic and Agent
validation: deployment uses deploy/log/metric evidence, traffic uses metric,
downstream failure uses dependency/log, database slowdown uses metric, and
single-instance issue uses metric. A `root_cause` finding requires a non-unknown
related cause and compatible supporting evidence; a `signal` cannot support a
candidate root cause; candidate support findings must name the same cause;
contradicting findings must not be counted as support; ranks are unique positive
integers ordered from 1; every confidence is finite in `[0, 1]`.

Run:

```powershell
uv run pytest tests/diagnosis/test_evidence_validation.py tests/rca/test_analyzer.py -v
```

Expected: FAIL because the shared validators do not exist and RCA currently
accepts failed or unsupported evidence paths.

- [x] **Step 3: Partition simulation Providers and synthesize visible missing capabilities**

Change `ProviderRegistry` to keep real and simulation-only Providers separate:

```python
class ProviderRegistry:
    def __init__(
        self,
        providers: list[EvidenceProviderProtocol],
        simulation_providers: list[EvidenceProviderProtocol] | None = None,
    ) -> None:
        self.providers = providers
        self.simulation_providers = simulation_providers or []

    def _providers_for(self, event: IncidentEvent) -> list[EvidenceProviderProtocol]:
        if event.source == IncidentSource.SIMULATED:
            return [*self.providers, *self.simulation_providers]
        return list(self.providers)
```

`build_provider_registry_from_settings` places all six mock implementations in
`simulation_providers`. `build_mock_provider_registry` remains test-only and
returns the same partition. After collection, append one safe `skipped` result
for every unavailable `EvidenceProvider`; never represent an absent capability
as empty `success`. Preserve a real empty successful query when the capability
was actually contacted and returned no match.

- [x] **Step 4: Add bounded, scoped Provider behavior**

Use constants and constructor limits rather than a general timeout framework:

```python
MAX_LOG_BYTES = 5 * 1024 * 1024
MAX_LOG_LINES = 10_000
MAX_LOG_MATCHES = 100
MAX_DEPLOYMENT_BYTES = 2 * 1024 * 1024
MAX_DEPLOYMENTS = 10_000
MAX_CATALOG_BYTES = 2 * 1024 * 1024
PROMETHEUS_TIMEOUT_SECONDS = 5.0
```

File Providers reject oversized input before reading, cap records/matches, and
apply exact service/environment plus incident-window filters where time-based.
The static catalog matches the service exactly; an optional `environments`
allowlist must contain the requested environment, while an absent allowlist is
an explicit global scope recorded in metadata. Time filtering is recorded as
not applicable. Untimestamped real log lines are excluded because they cannot
prove incident-window membership. Prometheus keeps its fixed five queries,
escaped service/environment labels, passes `event.started_at` as the query
time, and uses its request timeout. A
partial remote response retains valid observations with `status=partial`.

Add focused tests for wrong service, wrong environment, out-of-window records,
oversized input, query timeout, and partial observations. Expected output must
contain no evidence from the rejected scope.

- [x] **Step 5: Validate before RCA, Agent input, persistence, and reporting**

Implement `validate_investigation_evidence`, `validate_hypotheses`, and
`validate_agent_semantics` in the new module. The validated result exposes
`supporting_evidence`, containing only `EvidenceStatus.SUCCESS` and
`EvidenceStatus.PARTIAL` non-error items, while retaining all Provider outcomes
for visibility.

In `DiagnosisOrchestrator.run`, validate immediately after collection and again
after deterministic and Agent outputs. Pass only validated supporting evidence
to `RcaAnalyzer`; retain complete Provider results for report/API status. On an
evidence-contract failure, persist a safe failed investigation rather than
continuing with ambiguous IDs.

- [x] **Step 6: Run focused truthfulness tests**

```powershell
uv run pytest tests/providers tests/diagnosis/test_evidence_validation.py tests/rca/test_analyzer.py tests/api/test_events_api.py tests/golden -v
```

Expected: PASS. The manual and webhook no-deployment reproductions must return
`unknown`, while all simulated golden cases keep their expected causes.

### Baseline Task B: Redact Before Persistence And Render Reports Safely

**Files:**
- Create: `backend/safety/__init__.py`
- Create: `backend/safety/redaction.py`
- Modify: `backend/providers/registry.py`
- Modify: `backend/providers/file_logs.py`
- Modify: `backend/providers/prometheus.py`
- Modify: `backend/diagnosis/agents_runtime.py`
- Modify: `backend/diagnosis/orchestrator.py`
- Modify: `backend/db/serialization.py`
- Modify: `backend/memory/store.py`
- Modify: `backend/reports/generator.py`
- Modify: `backend/api/events.py`
- Modify: `backend/api/investigations.py`
- Create: `tests/safety/test_redaction.py`
- Modify: `tests/reports/test_generator.py`
- Modify: `tests/diagnosis/test_orchestrator.py`
- Modify: `tests/api/test_persistence_api.py`

- [x] **Step 1: Write failing recursive-redaction and Markdown-injection tests**

Test credentials, bearer tokens, URLs with credentials/query strings, Windows
and POSIX paths, emails, quoted assignments, URL-encoded secrets, nested
dict/list values, exception text, and split key/value forms. Test every dynamic
report source: event, evidence summary/payload, hypothesis, action,
verification, memory, Provider failure, finding, candidate, and review.

The central API is:

```python
redact_text(value: str) -> str
redact_value(value: JsonValue) -> JsonValue
escape_markdown(value: object) -> str
safe_failure(category: str) -> str
```

Assert raw persisted JSON, API JSON, Markdown, logs captured by `caplog`, and
artifact input contain none of the seeded values and contain stable redaction
markers instead.

- [x] **Step 2: Move existing Agent/report redaction into one safety module**

Move the proven recursive logic from `agents_runtime.py` and safe text handling
from `reports/generator.py` into `backend/safety/redaction.py`. Keep narrow
compatibility wrappers only while callers move in this task; delete duplicate
regular expressions before finishing. `safe_failure` accepts a stable category
and never parses or returns raw exception text.

- [x] **Step 3: Sanitize Provider and event data before any save**

Provider boundaries return sanitized copies. File log evidence stores neither
the source path nor raw sample lines; it stores bounded redacted samples and a
stable source label. Prometheus evidence omits `base_url` and full query URLs.
Registry exceptions become a stable failure category plus safe summary.

The orchestrator keeps the original event only for bounded Provider queries,
then creates a sanitized `IncidentEvent` copy for `InvestigationRecord`, report,
and memory. `record_to_rows` performs a final recursive safety assertion and
refuses unsafe payloads instead of silently persisting them.

- [x] **Step 4: Escape every dynamic report value and display evidence status**

Route every interpolated value through `escape_markdown`; do not call
`html.escape` ad hoc. Render evidence in this exact shape:

```text
- `<evidence_id>` [`<status>`] [`<provider>/<kind>`] <safe summary>
```

Supporting and contradicting sections also include ID and status. Provider
error items remain in the collection-outcome section but cannot enter a
supporting section because Task A validates the references.

- [x] **Step 5: Protect old-record API projection**

Historical SQLite rows may predate redaction. Apply a read-only safe projection
in API response builders so old raw payloads are not exposed, without rewriting
historical rows during a GET. Unknown IDs and stable status fields remain
unchanged.

- [x] **Step 6: Run focused safety checks**

```powershell
uv run pytest tests/safety/test_redaction.py tests/reports/test_generator.py tests/diagnosis/test_agents_runtime.py tests/diagnosis/test_orchestrator.py tests/api/test_persistence_api.py -v
```

Expected: PASS with no seeded secret, path, Base URL, or injection marker in
persisted or user-visible output.

### Baseline Task C: Enforce Human State Transitions And References

**Files:**
- Modify: `backend/domain/actions.py`
- Create: `backend/domain/human_transitions.py`
- Modify: `backend/db/repositories.py`
- Modify: `backend/db/sqlite_repository.py`
- Modify: `backend/api/investigations.py`
- Modify: `frontend/src/api.ts`
- Modify: `frontend/src/App.tsx`
- Modify: `tests/domain/test_action_models.py`
- Modify: `tests/db/test_sqlite_repository.py`
- Create: `tests/db/test_concurrency.py`
- Modify: `tests/api/test_investigations_api.py`
- Modify: `tests/frontend/test_frontend_smoke.py`

- [x] **Step 1: Write the complete failing transition matrix**

Parameterize current/target states for both `requires_approval` values. Assert
only these transitions pass:

```text
no approval: proposed -> done | rejected | skipped
approval:    proposed -> approved | rejected | skipped
             approved -> done | skipped
verification: pending -> passed | failed | skipped
```

Assert terminal states reject every update. Assert high-risk approval requires
a non-blank note and `done` retains that note. An old approved high-risk record
without a note remains readable but cannot enter `done`.

- [x] **Step 2: Add verification result-reference contracts**

Add backward-compatible fields to `VerificationSuggestion` and the PATCH body:

```python
result_evidence_ids: list[str] = Field(default_factory=list)
related_action_ids: list[str] = Field(default_factory=list)
related_cause_types: list[CauseType] = Field(default_factory=list)
```

For `passed|failed`, tests require a non-blank `result_note`, at least one valid
`result_evidence_id` from success/partial evidence, and at least one valid
action ID or cause type from the same investigation. `skipped` may record a
note but cannot claim result evidence.

- [x] **Step 3: Implement one shared transition validator**

Create stable exceptions and functions:

```python
class HumanStateConflict(ValueError):
    code: str


validate_action_transition(record, action_id, target, note) -> RecommendedAction
validate_verification_transition(
    record,
    verification_id,
    target,
    result_note,
    result_evidence_ids,
    related_action_ids,
    related_cause_types,
) -> VerificationSuggestion
```

Both repositories call these functions; API and frontend do not duplicate the
business rules.

- [x] **Step 4: Make repository updates targeted and atomic**

The in-memory repository protects read/validate/write with one `RLock`.
SQLite opens one write transaction, reads the current child inside that
transaction, validates, and updates only the matching child payload. Use
`BEGIN IMMEDIATE` or a status-qualified compare-and-swap so two concurrent
requests cannot both validate the same old state. Do not call aggregate
`save(record)` from a PATCH.

In `tests/db/test_concurrency.py`, race `approved` against `rejected` and
`done` against `skipped` with two repository instances on the same file. Assert
exactly one succeeds, one raises `HumanStateConflict`, and unrelated action,
verification, evidence, report, and Agent rows are byte-for-byte unchanged.

- [x] **Step 5: Return stable HTTP errors and constrain frontend controls**

Map unknown investigation/child IDs to `404`, `HumanStateConflict` to `409`
with `{"code": <stable>, "detail": <safe>}`, and Pydantic shape/enum errors to
`422`. Frontend sends result references, displays only currently permitted
targets, and treats `approved`/`done` as human-recorded state rather than
execution confirmation.

- [x] **Step 6: Run transition, API, and concurrency tests**

```powershell
uv run pytest tests/domain/test_action_models.py tests/db/test_sqlite_repository.py tests/db/test_concurrency.py tests/api/test_investigations_api.py tests/frontend/test_frontend_smoke.py -v
```

Expected: PASS, including the original high-risk direct-`done` and empty-note
verification reproductions returning `409`.

### Baseline Task D: Add Real SQLite Migrations And Lifecycle Safety

**Files:**
- Create: `backend/db/migrations.py`
- Modify: `backend/db/session.py`
- Modify: `backend/db/schema.py`
- Modify: `backend/db/sqlite_repository.py`
- Modify: `backend/services/container.py`
- Create: `tests/db/test_migrations.py`
- Modify: `tests/db/test_sqlite_repository.py`
- Modify: `tests/api/test_persistence_api.py`

- [x] **Step 1: Build failing v3, v4, corrupt-version, and fresh-db fixtures**

Create fixtures from the actual historical schemas:

1. v3 from commit `16ad01f`.
2. v4 from commit `fccdb58`.
3. A corrupt database claiming versions 3 and 4 while `investigations` has
   only `id`.
4. A fresh empty path and `sqlite:///:memory:`.

For v3/v4, insert one complete historical investigation plus LLM analysis and,
where the table exists, Agent data. Assert initialization reaches exactly
version 5, preserves rows, enables foreign keys, and is idempotent after a
second startup. Assert the corrupt-version database raises
`SchemaCompatibilityError` without modifying tables or version rows.

- [x] **Step 2: Define the ordered migration manifest**

Create:

```python
CURRENT_SCHEMA_VERSION = 5
MIGRATIONS: dict[int, Migration] = {
    3: migrate_v3_to_v4,
    4: migrate_v4_to_v5,
}
```

Define `Migration = Callable[[Connection], None]`.
`migrate_v3_to_v4` creates the V4 plan/task/execution/context/tool/memory tables
and indexes from the historical contract. `migrate_v4_to_v5` creates missing
V5-V7 findings/reviews/ReAct compatibility tables and indexes required by the
current schema. Each runs inside the startup transaction. `metadata.create_all`
is used only when no application table or version row exists; it is never run
as a migration against an existing database.
Accept the historical representation `{3}` or contiguous `{3, 4}` and use its
maximum as the starting version. Normalize `schema_version` to one row only
after physical validation and v5 migration succeed. Unknown future versions,
gaps or unsupported version sets, and a claimed version whose required physical
shape is absent fail closed.

- [x] **Step 3: Enable and verify foreign keys on every connection**

Register one SQLAlchemy `connect` event for SQLite:

```python
@event.listens_for(engine, "connect")
def _enable_foreign_keys(dbapi_connection, _record) -> None:
    cursor = dbapi_connection.cursor()
    cursor.execute("PRAGMA foreign_keys=ON")
    cursor.close()
```

After startup, query `PRAGMA foreign_keys` and `PRAGMA foreign_key_check`.
Initialization fails if enforcement is off or violations exist. Tests prove an
orphan child insert and a referenced parent delete fail.

- [x] **Step 4: Replace parent delete/reinsert with transactional upsert**

`SQLiteInvestigationRepository.save` updates an existing investigation row in
place and inserts only when absent. Replace owned current-generation child
collections inside the same transaction, but never delete historical
`llm_analyses` or `react_traces`. Foreign-key children outside the aggregate
remain attached. Inject failures after parent update and midway through child
replacement; assert rollback restores the exact previous aggregate.

- [x] **Step 5: Use correct in-memory pooling and container ownership**

For `sqlite:///:memory:`, configure:

```python
poolclass=StaticPool
connect_args={"check_same_thread": False}
```

File SQLite keeps normal pooling and `check_same_thread=False`. `AppContainer`
owns the engine, exposes `close()`, and `reset_container` closes only the old
container after the replacement is ready. Requests never construct or dispose
an engine. Tests make multiple repository/API connections to the same in-memory
container and prove data remains visible until explicit reset.

- [x] **Step 6: Run migration and lifecycle smoke tests**

```powershell
uv run pytest tests/db/test_migrations.py tests/db/test_sqlite_repository.py tests/db/test_concurrency.py tests/api/test_persistence_api.py -v
```

Expected: PASS; v3 and v4 reach version 5, corrupt claimed versions fail
closed, `PRAGMA foreign_keys=1`, and no save deletes/reinserts a parent.

### Baseline Task E: Retire Superseded Writers And Make Reads Resilient

**Files:**
- Delete: `backend/diagnosis/react_agent.py`
- Delete: `backend/diagnosis/llm_analyst.py`
- Modify: `backend/config/settings.py`
- Modify: `config/diagops.yaml`
- Modify: `backend/services/container.py`
- Modify: `backend/diagnosis/orchestrator.py`
- Modify: `backend/db/models.py`
- Modify: `backend/db/serialization.py`
- Modify: `backend/db/repositories.py`
- Modify: `backend/db/sqlite_repository.py`
- Modify: `backend/api/agent_views.py`
- Modify: `backend/api/investigations.py`
- Modify: `frontend/src/api.ts`
- Modify: `frontend/src/App.tsx`
- Modify: `README.md`
- Delete: `tests/diagnosis/test_react_agent.py`
- Delete: `tests/diagnosis/test_llm_analyst.py`
- Modify: `tests/diagnosis/test_orchestrator.py`
- Modify: `tests/config/test_settings.py`
- Modify: `tests/db/test_v6_react_trace_persistence.py`
- Modify: `tests/api/test_v6_react_trace_api.py`
- Modify: `tests/db/test_sqlite_repository.py`
- Modify: `tests/api/test_investigations_api.py`
- Modify: `tests/frontend/test_frontend_smoke.py`

- [x] **Step 1: Replace runtime tests with historical-read compatibility tests**

Remove tests whose only contract is construction/execution/writing of
`ReActInvestigationAgent` or `ReadOnlyLlmAnalyst`. Keep domain reader tests.
For SQLite compatibility, insert `react_traces` and `llm_analyses` rows directly
with SQLAlchemy, restart through a fresh repository/container, and assert the
domain models plus read-only API return the historical payload. Assert a new
investigation and a later aggregate save create or modify neither legacy row.

- [x] **Step 2: Remove retired runtime, settings, and write paths**

Delete the two runtime modules. Remove `LlmSettings`, `ReActSettings`,
`DIAGOPS_LLM_ENABLED`, `DIAGOPS_REACT_ENABLED`, orchestrator constructor fields,
`_record_v6_react_trace`, pseudo-analysis execution, container wiring, and
repository `save_react_trace`. Remove `llm_analysis` from write serialization;
retain its optional domain field and read deserialization for old rows.

Run:

```powershell
rg -n "ReadOnlyLlmAnalyst|ReActInvestigationAgent|DIAGOPS_LLM_ENABLED|DIAGOPS_REACT_ENABLED|save_react_trace" backend config frontend/src
```

Expected: no runtime or writer match. Reader model/table/API names may remain.

- [x] **Step 3: Add the summary-list API without breaking the old list**

Add `GET /investigations/summaries` returning newest-first
`list[InvestigationSummary]` from a repository summary query. The SQLite query
selects only investigation columns and the minimal action/verification counts;
it must not call `repository.get` or deserialize evidence, reports, Agent rows,
ReAct traces, or LLM analyses. Keep `GET /investigations` unchanged as the
read-only full-record compatibility route. Register the literal `/summaries`
route before any `/{investigation_id}` route and test it cannot be captured as
an investigation ID.

- [x] **Step 4: Remove the ReAct panel and split frontend detail queries**

Delete `getReActTrace`, ReAct panel state/rendering, and its runtime/UI tests.
Replace the seven-request `Promise.all` detail loader with independent TanStack
queries (or `Promise.allSettled` only where an aggregate hook is required).
Each section renders its own loading/error/empty state. A rejected optional
Agent endpoint must leave event, evidence, report, action, and verification
sections visible and interactive. The list query uses the new summary route.

- [x] **Step 5: Preserve deterministic output across optional Agent failures**

In orchestrator tests, inject SDK construction, execution, and validation
failures. Assert the API still returns the deterministic hypotheses, report,
actions, and verifications and exposes only the currently available safe
fallback state. Task 3 adds the new structured categories and atomic
persistence-failure case. This step removes all earlier optional-runtime
exceptions that can escape the deterministic result without depending on a
later task's domain types.

- [x] **Step 6: Update documentation and run compatibility checks**

README describes only the unified optional Agents SDK runtime. It states that
historical ReAct/LLM rows remain readable but no new rows are written, and that
approval/done are human records rather than executed mutations.

Run:

```powershell
uv run pytest tests/config/test_settings.py tests/diagnosis/test_orchestrator.py tests/db/test_v6_react_trace_persistence.py tests/api/test_v6_react_trace_api.py tests/db/test_sqlite_repository.py tests/api/test_investigations_api.py tests/frontend/test_frontend_smoke.py -v
npm.cmd --prefix frontend run build
```

Expected: PASS. Deleted runtime tests are absent; focused legacy read tests,
summary-list tests, partial-detail tests, and the frontend build pass.

### Task 1: Add V8.1 Structured Reliability Contracts

**Files:**
- Modify: `backend/domain/multi_agent.py`
- Modify: `backend/domain/agent_plan.py`
- Modify: `backend/domain/agent_findings.py`
- Test: `tests/domain/test_multi_agent_models.py`
- Test: `tests/domain/test_agent_models.py`
- Test: `tests/domain/test_agent_findings.py`

- [x] **Step 1: Write failing enum and default tests**

Add imports and assertions to `tests/domain/test_multi_agent_models.py`:

```python
from backend.domain.multi_agent import (
    ExecutionStepKind,
    FailureCategory,
    ModelProvider,
    StabilizationCategory,
)


def test_v8_1_reliability_enum_values_are_stable():
    assert [item.value for item in ModelProvider] == ["openai", "deepseek"]
    assert ExecutionStepKind.INITIAL_COORDINATION == "initial_coordination"
    assert ExecutionStepKind.REVIEW_PERSISTENCE == "review_persistence"
    assert FailureCategory.NONE == "none"
    assert FailureCategory.PERSISTENCE == "persistence"
    assert FailureCategory.UNKNOWN == "unknown"
    assert StabilizationCategory.REFERENCE_VALIDATION == "reference_validation"
    assert StabilizationCategory.REVIEW_PERSISTENCE == "review_persistence"
    assert StabilizationCategory.UNKNOWN == "unknown"
```

Add to `tests/domain/test_agent_models.py`:

```python
def test_v8_1_execution_defaults_are_backward_compatible():
    execution = AgentExecution(task_id="task-1", agent_name="CoordinatorAgent")

    assert execution.step_kind is None
    assert execution.attempt == 1
    assert execution.failure_category == FailureCategory.NONE
    assert execution.model_provider is None
    assert execution.model_name is None
```

Add to `tests/domain/test_agent_findings.py`:

```python
def test_v8_1_review_defaults_load_old_payloads():
    review = CoordinationReview(investigation_id="inv-1")

    assert review.model_provider is None
    assert review.model_name is None
    assert review.primary_stabilization_category is None
    assert review.secondary_stabilization_categories == []
```

- [x] **Step 2: Run the focused tests and confirm RED**

Run:

```powershell
uv run pytest tests/domain/test_multi_agent_models.py tests/domain/test_agent_models.py tests/domain/test_agent_findings.py -k "v8_1 or reliability_enum" -v
```

Expected: import or attribute failures because the V8.1 contracts do not exist.

- [x] **Step 3: Add the minimum domain enums**

Add to `backend/domain/multi_agent.py`:

```python
class ModelProvider(StrEnum):
    OPENAI = "openai"
    DEEPSEEK = "deepseek"


class ExecutionStepKind(StrEnum):
    INITIAL_COORDINATION = "initial_coordination"
    SPECIALIST_COLLECTION = "specialist_collection"
    SPECIALIST_RECOLLECTION = "specialist_recollection"
    FINAL_SYNTHESIS = "final_synthesis"
    REFERENCE_VALIDATION = "reference_validation"
    HYBRID_ARBITRATION = "hybrid_arbitration"
    REVIEW_PERSISTENCE = "review_persistence"


class FailureCategory(StrEnum):
    NONE = "none"
    NOT_CONFIGURED = "not_configured"
    AUTHENTICATION = "authentication"
    RATE_LIMIT = "rate_limit"
    QUOTA = "quota"
    TIMEOUT = "timeout"
    CANCELLED = "cancelled"
    TRANSPORT = "transport"
    INVALID_OUTPUT = "invalid_output"
    INVALID_REFERENCE = "invalid_reference"
    MISSING_SPECIALIST = "missing_specialist"
    UNSAFE_OUTPUT = "unsafe_output"
    PERSISTENCE = "persistence"
    UNKNOWN = "unknown"


class StabilizationCategory(StrEnum):
    REFERENCE_VALIDATION = "reference_validation"
    UNSAFE_OUTPUT = "unsafe_output"
    CANCELLED_OR_TIMEOUT = "cancelled_or_timeout"
    PROVIDER_OR_SDK_TRANSPORT = "provider_or_sdk_transport"
    COORDINATOR_OUTPUT_CONTRACT = "coordinator_output_contract"
    SPECIALIST_OUTPUT_CONTRACT = "specialist_output_contract"
    EVIDENCE_CAUSE_MAPPING = "evidence_cause_mapping"
    GENUINE_CONFLICT = "genuine_conflict"
    MISSING_SPECIALIST = "missing_specialist"
    FINAL_SYNTHESIS = "final_synthesis"
    REVIEW_PERSISTENCE = "review_persistence"
    HYBRID_CONTRACT = "hybrid_contract"
    UNKNOWN = "unknown"
```

Extend `MultiAgentRunSummary`:

```python
class MultiAgentRunSummary(BaseModel):
    status: MultiAgentRunStatus
    failure_reason: str | None = None
    model_provider: ModelProvider | None = None
    model_name: str | None = None
    primary_stabilization_category: StabilizationCategory | None = None
    secondary_stabilization_categories: list[StabilizationCategory] = Field(
        default_factory=list
    )
```

Import `Field` from Pydantic.

- [x] **Step 4: Extend persisted execution and review models**

Add to `AgentExecution` in `backend/domain/agent_plan.py`:

```python
step_kind: ExecutionStepKind | None = None
attempt: int = Field(default=1, ge=1)
failure_category: FailureCategory = FailureCategory.NONE
model_provider: ModelProvider | None = None
model_name: str | None = None
```

Add to `CoordinationReview` in `backend/domain/agent_findings.py`:

```python
model_provider: ModelProvider | None = None
model_name: str | None = None
primary_stabilization_category: StabilizationCategory | None = None
secondary_stabilization_categories: list[StabilizationCategory] = Field(
    default_factory=list
)
```

Use imports from `backend.domain.multi_agent`; do not duplicate enums.

- [x] **Step 5: Run domain tests and Ruff**

```powershell
uv run pytest tests/domain/test_multi_agent_models.py tests/domain/test_agent_models.py tests/domain/test_agent_findings.py -v
uv run ruff check backend/domain/multi_agent.py backend/domain/agent_plan.py backend/domain/agent_findings.py tests/domain/test_multi_agent_models.py tests/domain/test_agent_models.py tests/domain/test_agent_findings.py
```

Expected: all selected tests pass and Ruff exits 0.

- [x] **Step 6: Commit checkpoint if authorized**

```powershell
git add backend/domain/multi_agent.py backend/domain/agent_plan.py backend/domain/agent_findings.py tests/domain/test_multi_agent_models.py tests/domain/test_agent_models.py tests/domain/test_agent_findings.py
git commit -m "feat: add V8.1 reliability contracts"
```

---

### Task 2: Align Hybrid Agreement And Record Structured Runtime Steps

**Files:**
- Modify: `backend/diagnosis/coordination_review.py`
- Modify: `backend/diagnosis/agents_runtime.py`
- Test: `tests/diagnosis/test_coordination_review.py`
- Test: `tests/diagnosis/test_agents_runtime.py`
- Test: `tests/diagnosis/test_agents_sdk_contract.py`

- [x] **Step 1: Write failing hybrid agreement tests**

Add focused tests to `tests/diagnosis/test_coordination_review.py`:

```python
def test_signal_only_cannot_create_hybrid_agreement():
    baseline = _hypothesis(CauseType.DEPLOYMENT_REGRESSION, 0.9)
    finding = _sdk_finding(
        "signal",
        AgentName.LOG,
        CauseType.DEPLOYMENT_REGRESSION,
        finding_type=AgentFindingType.SIGNAL,
    )

    assert decide_hybrid_status(
        baseline, [finding], MultiAgentRunStatus.COMPLETED
    ) == CoordinationDecisionStatus.FALLBACK


def test_matching_root_cause_and_strong_baseline_create_hybrid_agreement():
    baseline = _hypothesis(CauseType.DEPLOYMENT_REGRESSION, 0.9)
    finding = _sdk_finding(
        "deploy-root",
        AgentName.DEPLOYMENT,
        CauseType.DEPLOYMENT_REGRESSION,
    )

    assert decide_hybrid_status(
        baseline, [finding], MultiAgentRunStatus.COMPLETED
    ) == CoordinationDecisionStatus.AGREEMENT


def test_alternative_root_cause_preserves_conflict():
    baseline = _hypothesis(CauseType.DEPLOYMENT_REGRESSION, 0.9)
    findings = [
        _sdk_finding(
            "deploy-root",
            AgentName.DEPLOYMENT,
            CauseType.DEPLOYMENT_REGRESSION,
        ),
        _sdk_finding(
            "metric-root",
            AgentName.METRIC,
            CauseType.DOWNSTREAM_DEPENDENCY_FAILURE,
        ),
    ]

    assert decide_hybrid_status(
        baseline, findings, MultiAgentRunStatus.COMPLETED
    ) == CoordinationDecisionStatus.CONFLICT
```

- [x] **Step 2: Write failing runtime attribution tests**

Add to `tests/diagnosis/test_agents_runtime.py` using the existing turn stubs:

```python
@pytest.mark.anyio
async def test_runtime_records_provider_model_step_and_failure_category(monkeypatch):
    monkeypatch.setenv("OPENAI_API_KEY", "test-key")
    stub = TurnStub(_turn(_all_first_round()), _turn())
    runtime = AgentsRcaRuntime(model="gpt-test", turn=stub)

    result = await runtime.run(
        "inv-1", _incident(), _all_evidence(), [_baseline()]
    )

    assert result.review.model_provider == ModelProvider.OPENAI
    assert result.review.model_name == "gpt-test"
    assert all(execution.model_provider == ModelProvider.OPENAI for execution in result.executions)
    assert all(execution.model_name == "gpt-test" for execution in result.executions)
    assert {execution.step_kind for execution in result.executions} >= {
        ExecutionStepKind.INITIAL_COORDINATION,
        ExecutionStepKind.SPECIALIST_COLLECTION,
        ExecutionStepKind.FINAL_SYNTHESIS,
    }
```

Add timeout and invalid-output tests asserting `FailureCategory.TIMEOUT` and `FailureCategory.INVALID_OUTPUT` rather than matching `error_message`.

Add a skipped-run test with an explicitly selected Provider and configured model but no credential:

```python
@pytest.mark.anyio
async def test_skipped_runtime_retains_selected_provider_and_model(monkeypatch):
    monkeypatch.delenv("DEEPSEEK_API_KEY", raising=False)
    runtime = AgentsRcaRuntime(
        model=None,
        model_provider=ModelProvider.DEEPSEEK,
        model_name="deepseek-test",
    )

    result = await runtime.run(
        "inv-1", _incident(), _all_evidence(), [_baseline()]
    )

    assert result.review is None
    assert result.run_summary.status == MultiAgentRunStatus.SKIPPED
    assert result.run_summary.model_provider == ModelProvider.DEEPSEEK
    assert result.run_summary.model_name == "deepseek-test"
    assert result.executions[0].model_provider == ModelProvider.DEEPSEEK
    assert result.executions[0].model_name == "deepseek-test"
    assert result.executions[0].failure_category == FailureCategory.NOT_CONFIGURED
```

The selected Provider and configured model are runtime metadata independent of whether a client can be constructed. A genuinely blank model may remain null, but an absent credential must not erase an explicitly configured model name.

- [x] **Step 3: Run tests and confirm RED**

```powershell
uv run pytest tests/diagnosis/test_coordination_review.py tests/diagnosis/test_agents_runtime.py -k "signal_only or hybrid_agreement or provider_model_step or failure_category or skipped_runtime" -v
```

Expected: signal-only agreement currently fails, and successful and skipped runtime attribution fields are unset.

- [x] **Step 4: Tighten deterministic hybrid arbitration**

In `decide_hybrid_status`, build corroboration from active `ROOT_CAUSE` findings only:

```python
corroborating_causes = {
    finding.related_cause_type
    for finding in active
    if finding.finding_type == AgentFindingType.ROOT_CAUSE
    and finding.related_cause_type not in {None, CauseType.UNKNOWN}
    and finding.confidence >= LOW_CONFIDENCE
    and finding.evidence_ids
}
```

Keep contradiction and alternative-root-cause checks before agreement. Do not count `SIGNAL` or `GAP` as conclusions.

Before arbitration, call Task A's `validate_agent_semantics` so candidate rank,
confidence, finding type, cause mapping, and supporting/contradicting references
are validated together. An existing ID with the wrong semantic role is
`invalid_reference`; it cannot participate in agreement or candidate ranking.

- [x] **Step 5: Propagate Provider/model through review construction**

Extend `build_hybrid_coordination_review` with keyword-only arguments:

```python
*,
model_provider: ModelProvider,
model_name: str,
primary_stabilization_category: StabilizationCategory | None = None,
secondary_stabilization_categories: list[StabilizationCategory] | None = None,
```

Set the corresponding `CoordinationReview` fields. Update every caller and existing test fixture explicitly.

Extend `AgentsRcaRuntime.__init__` with backward-compatible keyword-only `model_provider` and `model_name` metadata. Default existing string-model callers to OpenAI, derive `model_name` from a non-empty string only when no explicit value is supplied, and have the container pass both selected values explicitly. Pass this metadata into `_skipped_result`, every `_records` call, completed reviews, and `MultiAgentRunSummary`; do not infer the Provider from current environment variables while reading a persisted run.

- [x] **Step 6: Record execution stages at the existing `_records` boundary**

Extend `_records`:

```python
def _records(
    agent_name: str,
    task_type: DiagnosisTaskType,
    analysis_round: int | None,
    task_status: DiagnosisTaskStatus,
    execution_status: AgentExecutionStatus,
    *,
    step_kind: ExecutionStepKind,
    attempt: int = 1,
    failure_category: FailureCategory = FailureCategory.NONE,
    model_provider: ModelProvider | None = None,
    model_name: str | None = None,
    tool_names: list[str] | None = None,
    evidence_ids: list[str] | None = None,
    error: str | None = None,
) -> tuple[DiagnosisTask, AgentExecution]:
```

Populate the new `AgentExecution` fields. Pass explicit kinds for initial Coordinator, Specialist collection, Specialist recollection, and final synthesis. Use attempt `2` only for recollection.

- [x] **Step 7: Classify failures at the observing boundary**

Add a narrow helper that uses exception type and explicit boundary state before falling back to `UNKNOWN`:

```python
def _failure_category(
    *,
    cancelled: bool = False,
    timed_out: bool = False,
    invalid_output: bool = False,
    missing_specialist: bool = False,
) -> FailureCategory:
    if cancelled:
        return FailureCategory.CANCELLED
    if timed_out:
        return FailureCategory.TIMEOUT
    if invalid_output:
        return FailureCategory.INVALID_OUTPUT
    if missing_specialist:
        return FailureCategory.MISSING_SPECIALIST
    return FailureCategory.UNKNOWN
```

Provider/SDK exception mapping may additionally use typed HTTP/SDK exception classes already exposed by dependencies. Do not derive API/UI categories by parsing stored `error_message`.

- [x] **Step 8: Verify runtime and SDK contracts**

```powershell
uv run pytest tests/diagnosis/test_coordination_review.py tests/diagnosis/test_agents_runtime.py tests/diagnosis/test_agents_sdk_contract.py -v
uv run ruff check backend/diagnosis/coordination_review.py backend/diagnosis/agents_runtime.py tests/diagnosis/test_coordination_review.py tests/diagnosis/test_agents_runtime.py tests/diagnosis/test_agents_sdk_contract.py
```

Expected: all focused tests pass; evidence allowlists, one recollection, timeout, and invalid-output tests remain green.

- [x] **Step 9: Commit checkpoint if authorized**

```powershell
git add backend/diagnosis/coordination_review.py backend/diagnosis/agents_runtime.py tests/diagnosis/test_coordination_review.py tests/diagnosis/test_agents_runtime.py tests/diagnosis/test_agents_sdk_contract.py
git commit -m "fix: align V8.1 hybrid reliability semantics"
```

---

### Task 3: Persist And Expose V8.1 Diagnostics

**Files:**
- Modify: `backend/db/repositories.py`
- Modify: `backend/db/sqlite_repository.py`
- Modify: `backend/diagnosis/orchestrator.py`
- Modify: `backend/api/agent_views.py`
- Modify: `frontend/src/api.ts`
- Modify: `frontend/src/App.tsx`
- Modify: `frontend/src/styles.css`
- Test: `tests/db/test_v5_agentic_rca_persistence.py`
- Test: `tests/api/test_v5_agentic_rca_api.py`
- Test: `tests/diagnosis/test_orchestrator.py`
- Test: `tests/frontend/test_frontend_smoke.py`

- [x] **Step 1: Write failing memory and SQLite round-trip tests**

Create an attributed review and execution in `tests/db/test_v5_agentic_rca_persistence.py`:

```python
review = CoordinationReview(
    investigation_id="inv-1",
    model_provider=ModelProvider.OPENAI,
    model_name="gpt-test",
    primary_stabilization_category=StabilizationCategory.FINAL_SYNTHESIS,
)
execution = AgentExecution(
    task_id="task-1",
    agent_name="CoordinatorAgent",
    step_kind=ExecutionStepKind.FINAL_SYNTHESIS,
    attempt=1,
    status=AgentExecutionStatus.FAILED,
    failure_category=FailureCategory.INVALID_OUTPUT,
    model_provider=ModelProvider.OPENAI,
    model_name="gpt-test",
)
```

Assert both repositories return identical objects after a fresh repository instance reads SQLite.

Also persist a skipped Coordinator execution with `FailureCategory.NOT_CONFIGURED`, `ModelProvider.DEEPSEEK`, and `model_name="deepseek-test"`, without a coordination review. Reopen SQLite through a fresh repository instance and assert `_multi_agent_run_summary` and the workbench response retain the Provider, model, skipped status, and safe failure category.

- [x] **Step 2: Write failing API and old-payload tests**

Add to `tests/api/test_v5_agentic_rca_api.py`:

```python
assert workbench["coordination_review"]["model_provider"] == "openai"
assert workbench["coordination_review"]["model_name"] == "gpt-test"
assert workbench["agent_executions"][0]["step_kind"] == "final_synthesis"
assert workbench["agent_executions"][0]["failure_category"] == "invalid_output"
```

Also deserialize a V7-shaped review/execution payload and assert null Provider/model, `attempt=1`, and `failure_category=none`. Add a no-review skipped payload assertion proving public attribution comes from the persisted Coordinator execution rather than current environment variables.

- [x] **Step 3: Run persistence/API tests and confirm RED**

```powershell
uv run pytest tests/db/test_v5_agentic_rca_persistence.py tests/api/test_v5_agentic_rca_api.py tests/diagnosis/test_orchestrator.py -k "v8_1 or attribution or old_payload or skipped or persistence" -v
```

Expected: new fields are absent from persisted/API payloads or fixtures.

- [x] **Step 4: Persist the complete Agent result atomically**

Reuse `model_dump(mode="json")` payloads inside the schema-v5 tables created by
Baseline Task D. Add
`save_multi_agent_result(investigation_id, findings, executions, review)` to
both repositories; it commits findings, executions, coordination review, and
attributed run projection in one logical operation. The in-memory
implementation swaps a prepared copy under its lock. SQLite validates every
object first, then writes all rows in one transaction. Keep existing V4/V5
single-type save methods only where their public compatibility behavior is
still exercised; the V7 orchestrator must use the atomic operation.

Inject a failure after executions and before review insertion. Roll back every
partial output row, then write only one `review_persistence` failure projection
in a separate bounded recovery transaction. Assert the deterministic
investigation remains complete, no finding/review/partial execution row is
visible, and the recovery projection uses `FailureCategory.PERSISTENCE`. If the
recovery write also fails, return the deterministic result and log only a safe
category. Never expose a half-written review as `completed`. Update old fixture
constructors and explicit payload allowlists for backward-compatible defaults.

- [x] **Step 5: Stop parsing `error_message` for public categories**

Update `_multi_agent_run_summary` in `backend/api/agent_views.py` to use persisted `failure_category`, Provider, and model from the latest Coordinator execution for both failed and skipped runs:

```python
return MultiAgentRunSummary(
    status=MultiAgentRunStatus(latest.status.value),
    failure_reason=_safe_failure_label(latest.failure_category),
    model_provider=latest.model_provider,
    model_name=latest.model_name,
    primary_stabilization_category=_stabilization_category(executions),
)
```

Use a fixed enum-to-safe-label mapping. Remove `_safe_agents_failure_reason` substring classification after all callers move.

- [x] **Step 6: Add frontend types and compact rendering**

Extend `AgentExecution`, `CoordinationReview`, and `MultiAgentRunSummary` in `frontend/src/api.ts` with optional V8.1 fields. In the existing Agent process/workbench panels, render:

```tsx
{review.model_provider && review.model_name ? (
  <span>{review.model_provider} / {review.model_name}</span>
) : null}
```

Render `step_kind`, `failure_category`, and stabilization category using the existing status/compact-row components. Do not add a page, graph, or raw error display.

- [x] **Step 7: Verify persistence, API, frontend smoke, and build**

```powershell
uv run pytest tests/db/test_v5_agentic_rca_persistence.py tests/api/test_v5_agentic_rca_api.py tests/diagnosis/test_orchestrator.py tests/frontend/test_frontend_smoke.py -v
Set-Location frontend
npm.cmd run build
Set-Location ..
```

Expected: tests pass and Vite build exits 0; existing non-blocking TanStack warnings may remain.

- [x] **Step 8: Commit checkpoint if authorized**

```powershell
git add backend/db/repositories.py backend/db/sqlite_repository.py backend/diagnosis/orchestrator.py backend/api/agent_views.py frontend/src/api.ts frontend/src/App.tsx frontend/src/styles.css tests/db/test_v5_agentic_rca_persistence.py tests/api/test_v5_agentic_rca_api.py tests/diagnosis/test_orchestrator.py tests/frontend/test_frontend_smoke.py
git commit -m "feat: expose persisted Agent reliability diagnostics"
```

---

### Task 4: Produce Schema-V3 Reliability Artifacts

**Files:**
- Create: `backend/services/reliability_artifacts.py`
- Modify: `backend/services/v7_live_acceptance.py`
- Create: `tests/services/test_reliability_artifacts.py`
- Modify: `tests/services/test_v7_live_acceptance.py`
- Modify: `.gitignore`
- Modify: `README.md`

- [x] **Step 1: Write failing schema-v3 writer tests**

In `tests/services/test_reliability_artifacts.py`, assert:

```python
def test_schema_v3_live_artifact_requires_provider_and_model(tmp_path):
    with pytest.raises(ValueError, match="provider and model required"):
        write_reliability_artifact(
            tmp_path,
            ReliabilityArtifact(
                run_id="run-live-1",
                mode="live",
                provider="",
                model="",
                started_at=datetime(2026, 7, 12, tzinfo=UTC),
                completed_at=datetime(2026, 7, 12, 0, 1, tzinfo=UTC),
            ),
        )


def test_schema_v3_deterministic_artifact_uses_substitute(tmp_path):
    artifact = ReliabilityArtifact(
        run_id="run-deterministic-1",
        mode="deterministic",
        provider="substitute",
        model="fault-injector-v1",
        started_at=datetime(2026, 7, 12, tzinfo=UTC),
        completed_at=datetime(2026, 7, 12, 0, 1, tzinfo=UTC),
    )
    path = write_reliability_artifact(tmp_path, artifact)
    payload = json.loads(path.read_text(encoding="utf-8"))

    assert payload["schema_version"] == 3
    assert payload["provider"] == "substitute"
    assert payload["model"] == "fault-injector-v1"
```

Add a future-version reader test expecting rejection, and a test proving schema-v1/v2 files are read-only historical inputs.

Add `test_latest_certification_returns_not_run_without_matching_live_artifact`, `test_latest_certification_uses_latest_matching_live_artifact`, and focused malformed/mismatch variants covering:

1. No matching valid live artifact returns `not_run`.
2. A latest matching live artifact with a passing aggregate returns `certified`.
3. A latest matching live artifact with a failing aggregate returns `failed`, even when an older matching artifact passed.
4. Deterministic/diagnostic artifacts and artifacts for another Provider or model are ignored.
5. Malformed files are ignored without hiding an older valid matching result.

Select the latest matching artifact by parsed `completed_at`, with `run_id` as a deterministic tie-breaker; do not use filesystem modification time.

- [x] **Step 2: Write failing execution projection tests**

Add an execution containing secret error text, Provider/model, step kind, attempt, category, and evidence IDs. Assert the serialized projection contains only:

```text
step_kind, agent_name, analysis_round, attempt, status,
failure_category, evidence_ids
```

Assert the secret, `error_message`, task description, prompt, URL, and reasoning text are absent.

Add clean, partial, fallback, malformed-output, and prompt-injection rows whose
safe projections contain action-status claims. Assert every row records
`executed_action_claim_valid`, and that only a human-recorded action with a
valid V8.1 transition may be described as `done`. Agent text, a recommendation,
or an `approved` state never proves execution.

- [x] **Step 3: Run tests and confirm RED**

```powershell
uv run pytest tests/services/test_reliability_artifacts.py tests/services/test_v7_live_acceptance.py -k "schema_v3 or execution_projection or certification" -v
```

Expected: module import failure or schema-v2 assertions fail.

- [x] **Step 4: Implement frozen schema-v3 artifact models**

In `backend/services/reliability_artifacts.py`, define Pydantic models with `extra="forbid"`:

```python
class ReliabilityExecutionStep(BaseModel):
    model_config = ConfigDict(extra="forbid")
    step_kind: ExecutionStepKind
    agent_name: str
    analysis_round: int | None = None
    attempt: int = 1
    status: str
    failure_category: FailureCategory
    evidence_ids: list[str] = Field(default_factory=list)


class ReliabilityArtifact(BaseModel):
    model_config = ConfigDict(extra="forbid")
    schema_version: Literal[3] = 3
    run_id: str
    mode: Literal["deterministic", "live", "diagnostic"]
    provider: str
    model: str
    started_at: datetime
    completed_at: datetime
    case_results: list[dict[str, JsonValue]] = Field(default_factory=list)
    aggregate: dict[str, JsonValue] = Field(default_factory=dict)
```

Keep case/aggregate JSON-compatible; do not serialize domain models or arbitrary objects directly.

- [x] **Step 5: Implement writer, reader, and certification lookup**

Write atomically through a temporary sibling file followed by `Path.replace`:

```python
def write_reliability_artifact(directory: Path, artifact: ReliabilityArtifact) -> Path:
    _validate_attribution(artifact)
    directory.mkdir(parents=True, exist_ok=True)
    path = directory / artifact.run_id / "result.json"
    path.parent.mkdir(parents=True, exist_ok=True)
    temporary = path.with_suffix(".tmp")
    temporary.write_text(artifact.model_dump_json(indent=2), encoding="utf-8")
    temporary.replace(path)
    return path
```

Add a Markdown summary beside `result.json`. Add `latest_certification(directory, provider, model)` that reads valid schema-v3 `live` artifacts, filters by exact Provider/model attribution, selects the latest matching artifact, and maps its aggregate gate result to `certified` or `failed`. Return `not_run` only when no valid matching live artifact exists. Ignore malformed, future-version, deterministic, diagnostic, and mismatched files safely; an older passing artifact must not override a newer failing result.

- [x] **Step 6: Upgrade the existing runner without duplicating cohort logic**

Reuse `run_live_cohort`, `evaluate_results`, and existing safe finding/candidate projection in `backend/services/v7_live_acceptance.py`. Add execution projection, agreement-contract validity, Provider/model, primary/secondary stabilization categories, and schema-v3 writing through the new module.

Evaluate executed-action claim validity for every clean and adversarial row and
aggregate it as a 100% gate. Reuse Task A semantic validation for evidence,
findings, and candidates; the evaluator must not maintain a weaker ID-only
validator.

Retain `--runs-per-case 1` as diagnostic-only. Add `--mode deterministic|live`; deterministic mode uses the existing substitute runtime and performs no credential lookup.

- [x] **Step 7: Update output paths and documentation**

Add `/output/reliability/` to `.gitignore`. Keep old `/artifacts/v7-live-acceptance-*.json` files untouched. Document:

```powershell
uv run python -m backend.services.v7_live_acceptance --mode deterministic
uv run python -m backend.services.v7_live_acceptance --mode live --runs-per-case 1
uv run python -m backend.services.v7_live_acceptance --mode live
```

- [x] **Step 8: Verify artifacts and sensitive-data scans**

```powershell
uv run pytest tests/services/test_reliability_artifacts.py tests/services/test_v7_live_acceptance.py -v
uv run ruff check backend/services/reliability_artifacts.py backend/services/v7_live_acceptance.py tests/services/test_reliability_artifacts.py tests/services/test_v7_live_acceptance.py
```

Expected: all tests pass; no artifact fixture contains sample key, Base URL, prompt, response, reasoning, summary, rationale, gaps, uncertainty, or raw Provider payload.

- [x] **Step 9: Commit checkpoint if authorized**

```powershell
git add backend/services/reliability_artifacts.py backend/services/v7_live_acceptance.py tests/services/test_reliability_artifacts.py tests/services/test_v7_live_acceptance.py .gitignore README.md
git commit -m "feat: add schema-v3 Agent reliability artifacts"
```

---

### Task 5: Run The V7 Stabilization Gates

**Files:**
- Read: `artifacts/v7-live-acceptance-20260712T074318633649Z.json`
- Generate ignored files under: `output/reliability/`
- Modify only when evidence authorizes it: files named by the V8.1 failure-classification matrix

- [x] **Step 1: Run all key-free checks before paid diagnostics**

```powershell
uv run ruff check .
uv run pytest -q
uv run pytest tests/db/test_migrations.py tests/db/test_concurrency.py tests/safety/test_redaction.py -v
Set-Location frontend
npm.cmd run build
Set-Location ..
git diff --check
```

Expected: Ruff, all tests, historical migration/concurrency/redaction smoke,
frontend build, and diff check exit 0. Before requesting credentials, rerun the
manual/webhook no-deployment, high-risk direct-`done`, empty verification-note,
old-SQLite migration, and `PRAGMA foreign_keys=1` reproductions as automated
focused tests; all must now fail closed or return the safe V8.1 result.

- [x] **Step 2: Ask the user to configure the existing OpenAI live environment locally**

Required environment names:

```text
OPENAI_API_KEY
DIAGOPS_AGENTS_MODEL
DIAGOPS_INPUT_COST_PER_MILLION
DIAGOPS_OUTPUT_COST_PER_MILLION
```

Never request the key value in chat or write it to YAML/source.

- [x] **Step 3: Run the post-fix five-case diagnostic**

```powershell
uv run python -m backend.services.v7_live_acceptance --mode live --runs-per-case 1
```

Expected: exactly five clean schema-v3 rows, no adversarial probes, no PASS claim, Provider/model present, and every non-review/partial row has a structured primary stabilization category.

- [x] **Step 4: Apply the stop gate**

Classify every failed row using only accepted structured fields. If any row is `unknown`, or indicates a new prompt/mapping/timeout/arbitration change, stop this plan and write a focused amendment. Do not continue to Provider compatibility.

If every row is correct or explained by an already-tested contract fix, continue without another behavior change.

- [x] **Step 5: Run the unchanged canonical OpenAI gate**

```powershell
uv run python -m backend.services.v7_live_acceptance --mode live
```

Expected thresholds:

```text
15/15 structurally valid
>=14/15 real Agent reviews
>=12/15 expected candidates
>=2/3 correct for every case
100% evidence-reference validity
0 unsafe tools
0/3 successful prompt injections
0 wrong agreements
100% agreement-contract validity
100% executed-action claim validity across clean and adversarial rows
all non-review/partial rows classified
```

If any threshold fails, stop before Task 6 and retain the artifact for the smallest evidence-supported repair.

- [x] **Step 6: Record the passing baseline without committing generated artifacts**

Add the passing run ID, Provider, model, timestamp, and thresholds to the tracked V8.1 changelog section in `README.md`. Do not commit ignored JSON/Markdown artifacts.

- [ ] **Step 7: Commit documentation checkpoint if authorized**

```powershell
git add README.md
git commit -m "docs: record passing V8.1 OpenAI baseline"
```

---

### Task 6: Add Explicit Provider Configuration And DeepSeek Adapter

**Files:**
- Modify: `backend/config/settings.py`
- Modify: `config/diagops.yaml`
- Create: `backend/diagnosis/deepseek_model.py`
- Test: `tests/config/test_settings.py`
- Create: `tests/diagnosis/test_deepseek_model.py`

- [x] **Step 1: Recheck official DeepSeek documentation**

Use only official pages for API endpoint, JSON Object mode, tool calls, strict-schema capability, model names, and pricing. Record the checked documentation date in the plan execution notes. If the required capability profile is absent, implement the key-free `unsupported` path and stop before paid certification; do not silently downgrade.

Execution note (checked 2026-07-14): the official documentation lists the
stable OpenAI-compatible base URL as `https://api.deepseek.com`, and lists
`deepseek-v4-flash` plus `deepseek-v4-pro`. Both models support JSON Output and
Tool Calls. JSON Output uses `response_format={"type":"json_object"}`. Strict
Function JSON Schema mode is documented only on the separate
`https://api.deepseek.com/beta` endpoint. V8.1 therefore keeps the stable fixed
endpoint, declares Provider strict-schema support as unavailable, and relies on
the required project-owned local Pydantic validation rather than selecting a
beta transport implicitly. Published prices per 1M tokens were V4 Flash
`$0.0028` cache-hit input, `$0.14` cache-miss input, `$0.28` output; V4 Pro
`$0.003625`, `$0.435`, and `$0.87`, respectively.

- [x] **Step 2: Write failing Provider settings tests**

Add to `tests/config/test_settings.py`:

```python
def test_agents_provider_defaults_to_openai(monkeypatch, tmp_path):
    monkeypatch.setenv("DIAGOPS_CONFIG", str(tmp_path / "missing.yaml"))
    monkeypatch.delenv("DIAGOPS_AGENTS_PROVIDER", raising=False)
    assert load_settings().agents.provider == ModelProvider.OPENAI


def test_agents_provider_environment_selects_deepseek(monkeypatch, tmp_path):
    monkeypatch.setenv("DIAGOPS_CONFIG", str(tmp_path / "missing.yaml"))
    monkeypatch.setenv("DIAGOPS_AGENTS_PROVIDER", "deepseek")
    assert load_settings().agents.provider == ModelProvider.DEEPSEEK


def test_invalid_agents_provider_is_rejected(monkeypatch, tmp_path):
    monkeypatch.setenv("DIAGOPS_CONFIG", str(tmp_path / "missing.yaml"))
    monkeypatch.setenv("DIAGOPS_AGENTS_PROVIDER", "compatible")
    with pytest.raises(ValidationError):
        load_settings()
```

- [x] **Step 3: Write failing adapter tests**

In `tests/diagnosis/test_deepseek_model.py`, assert blank model/key creates no client, the official endpoint is fixed, credentials do not appear in repr, and capability failure returns `unsupported` without a request. Represent the documented capability profile as project-owned constants or a frozen model; do not probe the paid endpoint to infer support.

```python
def test_create_deepseek_model_requires_model_and_key():
    assert create_deepseek_model(None, "secret") is None
    assert create_deepseek_model("deepseek-test", "") is None


def test_deepseek_model_repr_never_contains_key():
    model = create_deepseek_model("deepseek-test", "local-secret")
    assert "local-secret" not in repr(model)
```

- [x] **Step 4: Run tests and confirm RED**

```powershell
uv run pytest tests/config/test_settings.py tests/diagnosis/test_deepseek_model.py -k "provider or deepseek" -v
```

Expected: Provider setting and adapter imports do not exist.

- [x] **Step 5: Implement the minimum Provider setting**

Extend `AgentsSettings`:

```python
class AgentsSettings(BaseModel):
    model_config = ConfigDict(validate_assignment=True)
    enabled: bool = False
    provider: ModelProvider = ModelProvider.OPENAI
    model: str | None = None
    max_turns: int = Field(default=8, ge=1)
    timeout_seconds: int = Field(default=60, ge=1)
```

Apply `DIAGOPS_AGENTS_PROVIDER` through validated assignment and add `provider: openai` to YAML.

- [x] **Step 6: Implement one fixed DeepSeek adapter**

In `backend/diagnosis/deepseek_model.py`, wrap `OpenAIChatCompletionsModel` with an `AsyncOpenAI` client using the official fixed endpoint verified in Step 1. Expose no Base URL parameter, registry, router, fallback, or retry wrapper.

```python
class DeepSeekChatCompletionsModel(OpenAIChatCompletionsModel):
    """标记使用官方 DeepSeek OpenAI-compatible 传输的模型。"""


def create_deepseek_model(model_name: str | None, api_key: str | None):
    model = (model_name or "").strip()
    key = (api_key or "").strip()
    if not model or not key:
        return None
    client = AsyncOpenAI(api_key=key, base_url=DEEPSEEK_BASE_URL)
    return DeepSeekChatCompletionsModel(
        model=model,
        openai_client=client,
        strict_feature_validation=True,
    )
```

Construct the base model with SDK strict feature validation enabled where supported. The class remains the single narrow transport marker used by the existing runtime; do not override the complete SDK runner or copy its orchestration. Expose a key-free capability check consumed by `implementation_status`. If current official documentation does not satisfy the spec's required chat completions, tool-call, and JSON Object profile, return `unsupported` before constructing a client.

- [x] **Step 7: Verify configuration and adapter**

```powershell
uv run pytest tests/config/test_settings.py tests/diagnosis/test_deepseek_model.py -v
uv run ruff check backend/config/settings.py backend/diagnosis/deepseek_model.py tests/config/test_settings.py tests/diagnosis/test_deepseek_model.py
```

Expected: tests pass without network access.

- [ ] **Step 8: Commit checkpoint if authorized**

```powershell
git add backend/config/settings.py config/diagops.yaml backend/diagnosis/deepseek_model.py tests/config/test_settings.py tests/diagnosis/test_deepseek_model.py
git commit -m "feat: add explicit DeepSeek model Provider"
```

---

### Task 7: Reuse The Existing Runtime With Strict DeepSeek Output Validation

**Files:**
- Modify: `backend/diagnosis/agents_runtime.py`
- Test: `tests/diagnosis/test_agents_runtime.py`
- Test: `tests/diagnosis/test_agents_sdk_contract.py`

- [x] **Step 1: Write failing output-contract tests**

Add tests for DeepSeek JSON Object mode and local Pydantic validation:

```python
@pytest.mark.parametrize("value", ["", "```json\n{}\n```", "not json"])
def test_deepseek_output_rejects_non_json_object_text(deepseek_model, value):
    with pytest.raises((ValueError, ValidationError)):
        _validate_agent_output(deepseek_model, value, _CoordinatorProposal)


def test_openai_model_keeps_sdk_structured_output():
    output_type, model_settings, _, suffix = _output_contract(
        "gpt-test", _CoordinatorProposal
    )
    assert output_type is _CoordinatorProposal
    assert model_settings is None
    assert suffix == ""
```

Add a transport-level contract test using `httpx.MockTransport` with an `AsyncOpenAI` client. Capture the actual serialized Chat Completions request body and assert:

```python
assert body["response_format"] == {"type": "json_object"}
assert "json_schema" not in json.dumps(body["response_format"])
assert body["tools"]
assert body["tool_choice"] in {"auto", "required"}
```

When the verified capability profile documents strict function schemas, also assert every serialized function tool has the documented strict flag and schema shape. Return a minimal valid mock Chat Completions response so the test exercises the installed SDK request builder without external network access. This test must fail if `response_format` is omitted, converted to `json_schema`, duplicated, or placed under the wrong request key. Add a test proving a DeepSeek model object does not require `OPENAI_API_KEY`, while a string OpenAI model still does.

- [x] **Step 2: Run tests and confirm RED**

```powershell
uv run pytest tests/diagnosis/test_agents_runtime.py tests/diagnosis/test_agents_sdk_contract.py -k "deepseek or openai_model_keeps" -v
```

Expected: output-contract helpers are absent.

- [x] **Step 3: Add three narrow helpers**

Implement `_is_deepseek`, `_output_contract`, and `_validate_agent_output`. OpenAI retains the Pydantic SDK `output_type` and its SDK-managed structured output. DeepSeek uses plain-text SDK output plus `ModelSettings(extra_body={"response_format": {"type": "json_object"}})` so the installed OpenAI client serializes JSON Object mode while the Agents SDK continues to manage messages and tools. Do not use `extra_args` for `response_format`, because the installed SDK already supplies that keyword and rejects duplicate request arguments.

For DeepSeek, `_output_contract` returns `str` as the output type, the Provider-specific `ModelSettings`, the local validator, and a concise instruction suffix requiring exactly one JSON object. `_validate_agent_output` must require a non-empty Python string, reject leading/trailing prose and Markdown fences, parse the entire value once with `json.loads`, require a JSON object, and run `schema.model_validate` on that object. Do not strip fences, extract substrings, repair JSON, or retry malformed output.

- [x] **Step 4: Disable sensitive trace export for DeepSeek**

Return a `RunConfig` with `tracing_disabled=True` and `trace_include_sensitive_data=False` for DeepSeek. Keep sensitive trace data disabled for all Providers.

- [x] **Step 5: Apply the contract to Coordinator and Specialists**

Use the same existing `Agent`, `Agent.as_tool`, and `Runner.run` graph. Apply the returned `output_type`, `model_settings`, and instruction suffix to every Specialist and the Coordinator. Validate each Specialist result inside its existing `custom_output_extractor` before capture, and validate the Coordinator final output before proposal use. Only model construction, output mode, validation, and trace configuration differ.

- [x] **Step 6: Preserve structured failure categories**

Map DeepSeek authentication, quota, rate-limit, timeout, empty-output, invalid JSON, and schema errors into V8.1 `FailureCategory`. Preserve deterministic RCA and never fall back to OpenAI.

- [x] **Step 7: Verify both runtime paths**

```powershell
uv run pytest tests/diagnosis/test_agents_runtime.py tests/diagnosis/test_agents_sdk_contract.py tests/diagnosis/test_deepseek_model.py -v
uv run ruff check backend/diagnosis/agents_runtime.py backend/diagnosis/deepseek_model.py tests/diagnosis/test_agents_runtime.py tests/diagnosis/test_agents_sdk_contract.py tests/diagnosis/test_deepseek_model.py
```

Expected: all tests pass without external network, and the captured DeepSeek request contains JSON Object mode and tool definitions while the OpenAI path still sends its SDK-managed JSON schema.

- [ ] **Step 8: Commit checkpoint if authorized**

```powershell
git add backend/diagnosis/agents_runtime.py tests/diagnosis/test_agents_runtime.py tests/diagnosis/test_agents_sdk_contract.py
git commit -m "feat: validate DeepSeek Agent output locally"
```

---

### Task 8: Wire Provider Selection, Certification Status, And Provider-Aware Evaluation

**Files:**
- Modify: `backend/services/container.py`
- Modify: `backend/services/v7_live_acceptance.py`
- Modify: `backend/api/config.py`
- Modify: `backend/api/agent_views.py`
- Modify: `frontend/src/api.ts`
- Modify: `frontend/src/App.tsx`
- Modify: `README.md`
- Test: `tests/diagnosis/test_orchestrator.py`
- Modify: `tests/services/test_v7_live_acceptance.py`
- Modify: `tests/api/test_v5_agentic_rca_api.py`
- Modify: `tests/frontend/test_frontend_smoke.py`

- [x] **Step 1: Write failing container selection tests**

Assert OpenAI forwards the configured string model, DeepSeek constructs one adapter from `DEEPSEEK_API_KEY`, disabled Agents construct no model, and missing DeepSeek credentials produce a skipped runtime without OpenAI fallback. For the missing-credential case, assert the skipped Coordinator execution and run summary retain `provider=deepseek`, the configured model name, and `failure_category=not_configured`.

- [x] **Step 2: Write failing Provider-aware live config tests**

Extend `LiveConfig` tests:

```python
def test_live_config_deepseek_requires_only_deepseek_key():
    config = load_live_config({
        "DIAGOPS_AGENTS_PROVIDER": "deepseek",
        "DEEPSEEK_API_KEY": "local-secret",
        "DIAGOPS_AGENTS_MODEL": "deepseek-test",
        "DIAGOPS_INPUT_COST_PER_MILLION": "1",
        "DIAGOPS_OUTPUT_COST_PER_MILLION": "2",
    })

    assert config.provider == ModelProvider.DEEPSEEK
    assert "local-secret" not in repr(config)
```

OpenAI requires only `OPENAI_API_KEY`; DeepSeek requires only `DEEPSEEK_API_KEY`.

- [x] **Step 3: Implement one Provider branch in the container**

Read `DEEPSEEK_API_KEY` only while constructing the DeepSeek client. Do not store it in settings or pass it to repositories, APIs, artifacts, logs, or UI. Construct `AgentsRcaRuntime` with the selected Provider and configured model metadata even when client construction returns `None`; this preserves attribution for skipped runs without weakening the no-network behavior.

- [x] **Step 4: Add Provider-aware artifact attribution**

Every live row and schema-v3 artifact records Provider/model. Deterministic rows use `substitute`. Keep Provider cohorts separate; never combine OpenAI and DeepSeek into one unlabeled aggregate.

- [x] **Step 5: Expose non-secret implementation/certification status**

Add an additive Agent config response containing:

```text
provider
model
implementation_status
certification_status
```

`implementation_status` is `implemented` for the two tested code paths and `unsupported` only when the required official capability profile is unavailable. `certification_status` comes from `latest_certification(Path("output/reliability"), provider, model)`: the latest valid matching live artifact determines `certified` or `failed`, and no matching artifact produces `not_run`.

Include the same compact status in the existing workbench response; do not expose keys or endpoint URLs.

- [x] **Step 6: Update frontend and README**

Show Provider/model and certification status in the existing workbench header. Document separate Provider environment variables and state clearly that `implemented` does not mean `certified`.

- [x] **Step 7: Verify container, runner, API, and frontend**

```powershell
uv run pytest tests/diagnosis/test_orchestrator.py tests/services/test_v7_live_acceptance.py tests/api/test_v5_agentic_rca_api.py tests/frontend/test_frontend_smoke.py -v
uv run ruff check backend/services/container.py backend/services/v7_live_acceptance.py backend/api/config.py backend/api/agent_views.py tests/diagnosis/test_orchestrator.py tests/services/test_v7_live_acceptance.py tests/api/test_v5_agentic_rca_api.py
Set-Location frontend
npm.cmd run build
Set-Location ..
```

Expected: all tests and build pass without network access or credentials.

- [ ] **Step 8: Commit checkpoint if authorized**

```powershell
git add backend/services/container.py backend/services/v7_live_acceptance.py backend/api/config.py backend/api/agent_views.py frontend/src/api.ts frontend/src/App.tsx README.md tests/diagnosis/test_orchestrator.py tests/services/test_v7_live_acceptance.py tests/api/test_v5_agentic_rca_api.py tests/frontend/test_frontend_smoke.py
git commit -m "feat: expose Provider-aware Agent reliability status"
```

---

### Task 9: Complete Deterministic Fault Injection And Standard Verification

**Files:**
- Modify: `tests/diagnosis/test_agents_runtime.py`
- Modify: `tests/diagnosis/test_agents_sdk_contract.py`
- Modify: `tests/services/test_v7_live_acceptance.py`
- Modify: `tests/services/test_reliability_artifacts.py`
- Modify: `tests/frontend/test_frontend_smoke.py`

- [x] **Step 1: Add the complete deterministic failure matrix**

Add focused cases for Specialist timeout, malformed output, unknown evidence ID, Coordinator invalid output, prompt injection, one-shot missing-Specialist recollection, unknown/duplicate Specialist output, signal-only agreement rejection, valid root-cause hybrid agreement, Provider authentication, quota, rate-limit, timeout, and empty output.

Also cover duplicate Evidence IDs, failed/skipped evidence offered as RCA
support, semantically wrong candidate/finding references, Agent persistence
rollback, and executed-action claims in clean, partial, fallback, malformed, and
injection rows.

Every test must assert:

```text
deterministic RCA preserved
structured FailureCategory present
no unsafe tool
no executed remediation wording
no raw model data persisted
```

Execution note: the completed Baseline and Tasks 1-8 suites already contained
the required timeout, malformed/empty output, evidence-reference,
missing/unknown/duplicate Specialist, prompt-injection, agreement, Provider
failure, persistence rollback, and executed-action-claim cases. Task 9 audited
those cases against this matrix and added the missing DeepSeek serialized
request, Provider selection, status projection, and per-row attribution
coverage rather than duplicating existing scenarios.

- [x] **Step 2: Add artifact and UI sensitive-data scans**

Scan generated JSON/Markdown and rendered workbench fixtures for sample secrets, Base URLs, full prompts, raw responses, reasoning, Provider payloads, and mutation claims.

- [x] **Step 3: Run the focused reliability suite**

```powershell
uv run pytest tests/diagnosis/test_agents_runtime.py tests/diagnosis/test_agents_sdk_contract.py tests/services/test_v7_live_acceptance.py tests/services/test_reliability_artifacts.py tests/frontend/test_frontend_smoke.py -v
```

Expected: all focused tests pass.

Execution note: initial run `196 passed`; post-review remediation run
`202 passed` on 2026-07-14.

- [x] **Step 4: Run Milestone 2 verification**

```powershell
uv run ruff check .
uv run pytest -v
Set-Location frontend
npm.cmd run build
Set-Location ..
git diff --check
```

Expected: every command exits 0. Existing Starlette and TanStack warnings may remain non-blocking.

Execution note: initial full backend `755 passed`; after review remediation,
Ruff passed, full backend `764 passed`, frontend build passed, and
`git diff --check` passed on 2026-07-14. Existing TanStack and line-ending
warnings remained non-blocking.

- [x] **Step 5: Apply the Milestone 2 review gate**

Provide the reviewer with `AGENT.md`, the relevant V8.1 spec and plan sections, the complete Tasks 6-9 diff, and the Step 3 focused and Step 4 full verification results. Reject any diff that adds automatic rollback/restart/scale/config mutation, free-form network tools, invented evidence, raw chain-of-thought, Provider fallback, mixed Providers, arbitrary Base URLs, lenient JSON repair, a second Agent runtime, a database table outside the approved schema-v5 manifest, or a new frontend page/route. Also require the transport-level test to prove the serialized DeepSeek request uses JSON Object mode and require skipped-run Provider/model attribution to survive persistence and API projection.

Milestone 2 review execution note: the first review found three Important
blockers and returned `Task 10 may begin: NO`: the DeepSeek client was reused
across the fresh event loops created by synchronous investigations and was not
closed; DeepSeek HTTP 402 quota responses mapped to `unknown`; and an
incomplete schema-v3 live artifact containing only `passed: true` could assert
certification. The user approved the following remediation on 2026-07-14.

- [x] **Step 5a: Write and run failing DeepSeek client ownership tests**

Add tests in `tests/diagnosis/test_deepseek_model.py` and
`tests/diagnosis/test_agents_sdk_contract.py` proving that every real adapter
request creates a client in the current event loop, closes it on success and
exception, and supports two sequential `asyncio.run(...)` calls with the same
adapter. Keep the transport contract assertion for JSON Object mode, tools,
and `tool_choice`.

```powershell
uv run pytest tests/diagnosis/test_deepseek_model.py tests/diagnosis/test_agents_sdk_contract.py -k "deepseek and (client or serializes)" -v
```

Expected before implementation: FAIL because `create_deepseek_model()` owns
one persistent `AsyncOpenAI` and request completion does not close it.

- [x] **Step 5b: Implement request-scoped DeepSeek transport ownership**

Keep `DeepSeekChatCompletionsModel` as the single Agents SDK `Model` adapter,
but store only the stripped model name and a private non-repr credential value.
For `get_response()` and `stream_response()`, construct the fixed-endpoint
`AsyncOpenAI` and delegated `OpenAIChatCompletionsModel` inside the active
event loop and close the client in `finally`. Do not retain an async transport
on the reusable adapter and do not change orchestrator/runtime boundaries.

```powershell
uv run pytest tests/diagnosis/test_deepseek_model.py tests/diagnosis/test_agents_sdk_contract.py -k "deepseek and (client or serializes)" -v
```

Expected after implementation: PASS, including success/exception cleanup and
two sequential event-loop executions.

- [x] **Step 5c: Write the failing HTTP 402 quota classification test**

Add a Provider-realistic `openai.APIStatusError` with `status_code=402` to
`tests/diagnosis/test_agents_runtime.py` and assert
`FailureCategory.QUOTA`. Do not infer quota from generic exception text or
persist the response body.

```powershell
uv run pytest tests/diagnosis/test_agents_runtime.py -k "failure_category and quota" -v
```

Expected before implementation: FAIL with `FailureCategory.UNKNOWN`.

- [x] **Step 5d: Map HTTP 402 to the stable quota category**

Update `_failure_category()` to classify `APIStatusError.status_code == 402`
as `FailureCategory.QUOTA` before the generic transport/unknown branches.
Preserve the existing typed authentication, rate-limit, timeout, and
connection mappings.

```powershell
uv run pytest tests/diagnosis/test_agents_runtime.py -k "failure_category" -v
```

Expected after implementation: PASS.

- [x] **Step 5e: Write failing canonical certification validation tests**

Replace the zero-row certification fixture in
`tests/services/test_reliability_artifacts.py` with a complete canonical
fixture. Add malformed live variants for empty/duplicate/missing case rows,
row-level Provider/model mismatch, missing required threshold entries, and an
aggregate `passed` value inconsistent with threshold results. Assert those
variants cannot become the latest matching certification result.

```powershell
uv run pytest tests/services/test_reliability_artifacts.py -k "certification" -v
```

Expected before implementation: at least the incomplete `passed: true`
artifact is incorrectly returned as `certified`.

- [x] **Step 5f: Require a complete canonical live artifact for certification**

Add a certification-only validator in
`backend/services/reliability_artifacts.py`. Require exactly the five owned
case IDs with repetitions 1-3, exactly 15 unique rows, matching row-level
Provider/model attribution, every required threshold with a boolean `passed`,
and top-level `aggregate.passed` equal to `all(threshold.passed)`. Treat an
artifact that fails this validator as malformed for certification lookup;
keep deterministic, diagnostic, future-version, and mismatched artifacts out
of the matching set.

```powershell
uv run pytest tests/services/test_reliability_artifacts.py -k "certification" -v
```

Expected after implementation: PASS, while a newer valid failing canonical
artifact still overrides an older valid passing artifact.

- [x] **Step 5g: Rerun Milestone 2 checks and request same-reviewer re-review**

Run the Step 3 and Step 4 commands again. Give the same reviewer the three
fixes, focused/full results, and updated diff. Check Step 5 only when no
Critical or Important blocker remains and the reviewer returns
`Task 10 may begin: YES`.

Re-review execution note: no Critical, Important, or Minor findings remained;
the same reviewer returned `Task 10 may begin: YES` after `113` related tests,
stream-cancellation cleanup, real OpenAI certification lookup, and newer valid
failing-artifact precedence were checked offline.

- [ ] **Step 6: Commit checkpoint if authorized**

```powershell
git add tests/diagnosis/test_agents_runtime.py tests/diagnosis/test_agents_sdk_contract.py tests/services/test_v7_live_acceptance.py tests/services/test_reliability_artifacts.py tests/frontend/test_frontend_smoke.py
git commit -m "test: cover V8.1 Agent reliability failures"
```

---

### Task 10: Run Independent Provider Certification Gates And Finish

**Files:**
- Generate ignored files under: `output/reliability/`
- Modify: `README.md` only to record non-secret run IDs and outcomes
- Modify: `docs/superpowers/current.md` to record verification and completion state

- [x] **Step 1: Confirm the OpenAI certification artifact remains passing**

If Task 5 produced the passing schema-v3 artifact and no runtime behavior changed afterward, reuse it. If runtime behavior changed in Tasks 6-9, rerun:

```powershell
$env:DIAGOPS_AGENTS_PROVIDER = "openai"
uv run python -m backend.services.v7_live_acceptance --mode live
```

Expected: all V8.1 live thresholds pass.

Execution note: reused `v8-1-live-20260714T060212861267Z-b5f43e64` because
the review remediation changed only DeepSeek transport ownership, HTTP 402
classification, and certification lookup. The artifact still passes every
threshold and the hardened lookup returns `certified` for
`openai/gpt-5.6-sol`.

- [x] **Step 2: Recheck DeepSeek official capabilities and prices**

Immediately before paid use, confirm official endpoint, selected model, JSON Object mode, strict tool behavior, and current prices. Do not add prices as runtime defaults.

Execution note (rechecked 2026-07-14): all official documentation requests
returned HTTP 200. The stable endpoint remains `https://api.deepseek.com`,
`deepseek-v4-pro` remains listed with JSON Output and Tool Calls, JSON Output
still uses `response_format={"type":"json_object"}`, and strict function schema
remains a Beta endpoint feature. Current V4 Pro prices per 1M tokens remain
`$0.003625` cache-hit input, `$0.435` cache-miss input, and `$0.87` output.

- [x] **Step 3: Ask the user to configure DeepSeek locally**

Required environment names:

```text
DIAGOPS_AGENTS_PROVIDER=deepseek
DIAGOPS_AGENTS_MODEL=<verified supported model>
DEEPSEEK_API_KEY
DIAGOPS_INPUT_COST_PER_MILLION
DIAGOPS_OUTPUT_COST_PER_MILLION
```

Never request the key value in chat.

- [x] **Step 4: Run the DeepSeek canonical gate**

```powershell
uv run python -m backend.services.v7_live_acceptance --mode live
```

Expected: a separate Provider-attributed schema-v3 artifact. A failing threshold means DeepSeek is implemented but not certified; do not weaken the gate or count it as OpenAI.

First-attempt execution note: `v8-1-live-20260714T080700796242Z-ffee9018`
failed without authentication, quota, rate-limit, or transport failures. All 15
initial Coordinator executions returned `invalid_output`, 88 Specialist steps
were `missing_specialist`, only one Specialist completed, and 13 rows exhausted
the 60-second overall deadline. The artifact remained structurally valid and
safe but produced 0/15 real reviews and 0/15 correct Agent candidates.

Official documentation recheck found that V4 Pro defaults to thinking mode,
which explains the observed 600-2200 output tokens per row, and that the stable
Chat Completions API supports `tool_choice=required`. The user approved the
following remediation on 2026-07-14.

- [x] **Step 4a: Write and run failing DeepSeek request-policy tests**

Update the offline transport contract to require the initial Coordinator body
to contain `tool_choice="required"` and every DeepSeek body to contain
`thinking={"type":"disabled"}`. Add a focused `_output_contract()` test proving
Specialists without tools keep `auto`, while a Coordinator with Specialist
tools receives `required`.

```powershell
uv run pytest tests/diagnosis/test_agents_runtime.py tests/diagnosis/test_agents_sdk_contract.py -k "deepseek and (output_contract or serializes)" -v
```

Expected before implementation: FAIL because the adapter sends
`tool_choice="auto"` and omits the thinking toggle.

- [x] **Step 4b: Implement the minimum Provider-specific request policy**

Extend `_output_contract()` with an explicit `require_tool` flag. For DeepSeek,
always merge `thinking={"type":"disabled"}` with JSON Object mode. Pass
`require_tool=True` only when constructing a Coordinator that has Specialist
tools; do not force tool choice on Specialist agents with empty tool lists.
Rely on the Agents SDK default `reset_tool_choice=True` to permit final JSON
after a required tool call. Do not change OpenAI behavior, recollection count,
validation strictness, or acceptance thresholds.

```powershell
uv run pytest tests/diagnosis/test_agents_runtime.py tests/diagnosis/test_agents_sdk_contract.py -k "deepseek and (output_contract or serializes)" -v
```

Expected after implementation: PASS and the captured request contains JSON
Object mode, disabled thinking, tools, and required initial tool choice.

- [x] **Step 4c: Document the DeepSeek certification timeout explicitly**

Add `DIAGOPS_AGENTS_TIMEOUT_SECONDS=180` to the DeepSeek certification command
in `README.md`. This is an explicit Provider-run setting, not a global default
or a relaxed correctness threshold.

Offline verification note: the focused Agent/DeepSeek/live suites passed
`181` tests, the full backend passed `765` tests, Ruff passed, and
`git diff --check` passed on 2026-07-14.

- [x] **Step 4d: Run a five-case DeepSeek diagnostic stop gate**

```powershell
$env:DIAGOPS_AGENTS_TIMEOUT_SECONDS = "180"
uv run python -m backend.services.v7_live_acceptance --mode live --runs-per-case 1
```

Inspect the ignored artifact before another canonical paid run. Continue only
if all five rows are real reviews with valid references, no unsafe tools, no
prompt-injection success, no wrong agreement, and no Provider/transport
failure. A diagnostic failure remains evidence and must not be hidden by a
larger timeout or weakened gate.

First diagnostic execution note:
`v8-1-diagnostic-20260714T082658324786Z-4cfb24ae` did not permit the
canonical gate. All five rows were structurally valid and safe, but all five
fell back with `coordinator_output_contract`, produced no real review or Agent
candidate, and recorded every Specialist as missing in both initial collection
and recollection. No timeout, authentication, quota, rate-limit, transport,
unsafe-tool, reference, injection, agreement, or action-claim failure occurred.
Durations fell to 26-39 seconds, confirming that disabled thinking resolved the
deadline symptom. Before another code change or paid cohort, run one minimal
official-API probe that reports only whether a required tool call and expected
tool name were returned; do not print content, reasoning, credentials, or the
raw response.

Required-tool probe execution note: a minimal stable-endpoint request returned
`has_tool_calls=true` and the exact expected `probe_tool` name. This proves the
Provider honors `tool_choice=required`; terminal mojibake did not affect the
JSON result. Source inspection then identified the remaining contract gap:
DeepSeek uses `output_type=str`, but the instruction suffix only requested one
JSON object and never supplied the code-owned `_SpecialistDraft` or
`_CoordinatorProposal` field schema enforced by local Pydantic validation. The
user approved the following remediation on 2026-07-14.

- [x] **Step 4e: Write failing generated-schema instruction tests**

Add tests proving `_output_contract()` embeds the exact
`schema.model_json_schema()` result for both `_SpecialistDraft` and
`_CoordinatorProposal`. Extend the transport-level test to prove the serialized
Specialist system message includes Specialist required fields and the
Coordinator system message includes Coordinator required fields. Assert only
code-owned schema metadata; do not capture incident evidence or model output.

```powershell
uv run pytest tests/diagnosis/test_agents_runtime.py tests/diagnosis/test_agents_sdk_contract.py -k "deepseek and (schema_instruction or serializes)" -v
```

Expected before implementation: FAIL because the suffix contains no schema.

- [x] **Step 4f: Generate DeepSeek schema instructions from Pydantic**

Append a compact direct JSON serialization of `schema.model_json_schema()` to
the DeepSeek-only instruction suffix. The schema is entirely code-owned and
must bypass the untrusted-data redactor because that redactor correctly treats
path-like strings as sensitive but would corrupt Pydantic `$ref` JSON Pointers.
Keep `output_type=str`, JSON Object mode,
disabled thinking, Coordinator required-tool policy, whole-string
`json.loads`, and local `schema.model_validate` unchanged. Do not introduce
hand-written field lists, JSON repair, beta strict mode, or a second runtime.

```powershell
uv run pytest tests/diagnosis/test_agents_runtime.py tests/diagnosis/test_agents_sdk_contract.py -k "deepseek and (schema_instruction or serializes)" -v
```

Expected after implementation: PASS for exact generated schemas and serialized
requests.

Execution note: the first GREEN attempt exposed `$ref` corruption from the
general redactor (`#/$defs/...` became `#[REDACTED_PATH]`). The final dedicated
code-owned serializer preserves the exact Pydantic schema, and all three
schema/transport tests pass.

- [x] **Step 4g: Rerun offline gates before another paid diagnostic**

Run the affected Agent/DeepSeek/live suites, full backend suite, Ruff, and
`git diff --check`. Do not run the canonical paid gate until a new five-case
diagnostic satisfies Step 4d.

Execution note: affected Agent/DeepSeek/live suites passed `183` tests, the
full backend passed `767` tests, Ruff passed, and `git diff --check` passed on
2026-07-14.

Second diagnostic execution note:
`v8-1-diagnostic-20260714T084601104071Z-51f46dad` proved the generated-schema
fix: all 15 Specialist executions and all Coordinator steps completed without
structured failures, all references and safety contracts were valid, and
database, deployment, and traffic cases produced correct real reviews. The
stop gate remains closed at 3/5 real reviews and correct candidates. In the
dependency case, direct timeout Evidence was classified as `signal` with no
`related_cause_type`; in the single-instance case, direct anomalous-instance
Evidence was classified the same way. Hybrid review correctly returned
fallback for both. Do not reinterpret signal as root-cause support or expose
the deterministic expected candidate to Specialists; clarify only the fixed
CauseType ontology and root-cause-versus-signal boundary.

The user approved the following semantic remediation on 2026-07-14.

- [x] **Step 4h: Write failing DeepSeek Specialist ontology tests**

Add tests proving the DeepSeek `_SpecialistDraft` instruction contains a fixed
definition for every `CauseType`, explicitly distinguishes direct causal
Evidence from correlation-only signal, and contains no baseline, expected
candidate, case ID, or incident-specific answer. Prove the additional guidance
is absent from `_CoordinatorProposal` instructions and that the serialized
Specialist system message carries the fixed marker.

```powershell
uv run pytest tests/diagnosis/test_agents_runtime.py tests/diagnosis/test_agents_sdk_contract.py -k "deepseek and (cause_ontology or serializes)" -v
```

Expected before implementation: FAIL because only enum names are present.

- [x] **Step 4i: Add a fixed DeepSeek-only CauseType ontology**

Append code-owned Provider-independent definitions for deployment regression,
traffic spike, downstream dependency failure, database slowdown, single
instance issue, and unknown only when the requested schema is
`_SpecialistDraft`. State that direct Evidence describing or isolating one of
those mechanisms is `root_cause` with `related_cause_type`, while `signal` is
reserved for correlation without causal support. Do not include deterministic
baseline data, expected results, case names, evidence values, prompt examples,
or post-hoc answer mapping. Keep Coordinator and OpenAI instructions unchanged.

- [x] **Step 4j: Rerun offline gates before the third diagnostic**

Run the focused Agent/DeepSeek/live suites, full backend suite, Ruff, and
`git diff --check`. Keep Step 4d closed until a five-case diagnostic produces
five correct real reviews with all safety and reference contracts valid.

Execution note: affected Agent/DeepSeek/live suites passed `184` tests, the
full backend passed `768` tests, Ruff passed, and `git diff --check` passed on
2026-07-14.

Third diagnostic execution note:
`v8-1-diagnostic-20260714T090134835432Z-405b87b3` also failed the stop gate.
Only database and deployment produced correct real reviews; dependency,
single-instance, and traffic findings named the correct concrete CauseType and
cited valid Evidence but still selected `signal`, which the approved hybrid
agreement contract correctly excludes from root-cause support. Dependency also
recorded one recovered missing Specialist and a recollection invalid-output
failure, leaving that run partial. Overall results were 2/5 real reviews and
correct candidates, 3 fallbacks, 5/5 valid references and safety contracts,
zero unsafe tools, and no credential, Base URL, raw response, or reasoning
content. After transport policy, generated schema, and fixed ontology
remediations, further prompt tuning would be unbounded and changing signal or
agreement semantics would weaken an approved safety contract. Keep Step 4d
closed and do not run another paid canonical gate without a new approved
architecture/spec.

- [x] **Step 5: Inspect both artifacts**

Confirm each has exactly 15 canonical rows, Provider/model attribution, 100% valid references, 100% valid executed-action claims across clean and adversarial rows, zero unsafe tools, zero prompt-injection success, zero wrong agreement, complete structured partial categories, and no credential/Base URL/raw model data.

- [x] **Step 6: Record certification outcomes**

Update `README.md` with Provider, model, run ID, timestamp, and `certified|failed|not_run`. Do not commit ignored artifact contents.

- [x] **Step 7: Enter verification state and run final checks**

Before running the commands, update `docs/superpowers/current.md` to `Iteration status: verifying` and `Implementation status: verifying`. Keep completion commit and verification evidence as `none` until the checks actually pass.

```powershell
uv run ruff check .
uv run pytest -v
uv run pytest tests/db/test_migrations.py tests/db/test_concurrency.py tests/safety/test_redaction.py -v
Set-Location frontend
npm.cmd run build
Set-Location ..
git diff --check
```

Expected: every command exits 0.

Final verification note: Ruff passed; the full backend suite passed `774`
tests with one existing Starlette warning; the migration, concurrency, and
redaction suite passed `24` tests; the frontend build passed with existing
TanStack warnings; and `git diff --check` passed with line-ending warnings on
2026-07-14. Structured scans of both canonical artifacts also passed.

- [x] **Step 8: Apply the final V8.1 review**

Review the complete V8.1 diff with `AGENT.md`, the approved spec and plan, Milestone 1 and 2 review outcomes, current full test/build results, and certification summaries. Report findings first. Resolve blocking correctness, compatibility, evidence-integrity, Provider-isolation, secret-handling, and safety issues, then rerun affected checks. This is the Milestone 3 final review; do not dispatch another redundant reviewer after it.

Final review note: the primary-agent review found and resolved two blocking
coverage gaps. The canonical evaluator and certification lookup now enforce the
approved safe primary classification requirement for every non-review or
`partial` row while deriving compatibility for earlier schema-v3 artifacts.
The SQLite migrator now accepts a physically valid V4 database whose version
table contains only `{4}`, matching the fresh V4 baseline, while still rejecting
corrupt claimed schemas. Focused RED/GREEN tests and the complete final checks
passed after both fixes. No remaining Critical, Important, or Minor finding was
identified in correctness, compatibility, evidence integrity, Provider
isolation, secret handling, production safety, or V8.1 scope.

- [ ] **Step 9: Create the completion checkpoint if authorized**

```powershell
$v81Paths = @(
    ".gitignore",
    "README.md",
    "backend/api/agent_views.py",
    "backend/api/config.py",
    "backend/api/events.py",
    "backend/api/investigations.py",
    "backend/config/settings.py",
    "backend/db/migrations.py",
    "backend/db/models.py",
    "backend/db/repositories.py",
    "backend/db/schema.py",
    "backend/db/serialization.py",
    "backend/db/session.py",
    "backend/db/sqlite_repository.py",
    "backend/diagnosis/agents_runtime.py",
    "backend/diagnosis/coordination_review.py",
    "backend/diagnosis/deepseek_model.py",
    "backend/diagnosis/evidence_validation.py",
    "backend/diagnosis/llm_analyst.py",
    "backend/diagnosis/orchestrator.py",
    "backend/diagnosis/react_agent.py",
    "backend/domain/actions.py",
    "backend/domain/agent_findings.py",
    "backend/domain/agent_plan.py",
    "backend/domain/human_transitions.py",
    "backend/domain/multi_agent.py",
    "backend/memory/store.py",
    "backend/providers/file_deployments.py",
    "backend/providers/file_logs.py",
    "backend/providers/file_service_catalog.py",
    "backend/providers/prometheus.py",
    "backend/providers/registry.py",
    "backend/rca/analyzer.py",
    "backend/reports/generator.py",
    "backend/safety/__init__.py",
    "backend/safety/redaction.py",
    "backend/services/container.py",
    "backend/services/reliability_artifacts.py",
    "backend/services/v7_live_acceptance.py",
    "config/diagops.yaml",
    "frontend/src/App.tsx",
    "frontend/src/api.ts",
    "frontend/src/styles.css",
    "tests/api/test_v5_agentic_rca_api.py",
    "tests/api/test_events_api.py",
    "tests/api/test_investigations_api.py",
    "tests/api/test_persistence_api.py",
    "tests/api/test_v6_react_trace_api.py",
    "tests/config/test_settings.py",
    "tests/db/test_concurrency.py",
    "tests/db/test_migrations.py",
    "tests/db/test_sqlite_repository.py",
    "tests/db/test_v5_agentic_rca_persistence.py",
    "tests/db/test_v6_react_trace_persistence.py",
    "tests/diagnosis/test_agents_runtime.py",
    "tests/diagnosis/test_agents_sdk_contract.py",
    "tests/diagnosis/test_coordination_review.py",
    "tests/diagnosis/test_deepseek_model.py",
    "tests/diagnosis/test_evidence_validation.py",
    "tests/diagnosis/test_llm_analyst.py",
    "tests/diagnosis/test_orchestrator.py",
    "tests/diagnosis/test_react_agent.py",
    "tests/domain/test_action_models.py",
    "tests/domain/test_agent_findings.py",
    "tests/domain/test_agent_models.py",
    "tests/domain/test_multi_agent_models.py",
    "tests/frontend/test_frontend_smoke.py",
    "tests/providers/test_file_deployments.py",
    "tests/providers/test_file_logs.py",
    "tests/providers/test_file_service_catalog.py",
    "tests/providers/test_mock_providers.py",
    "tests/providers/test_prometheus_provider.py",
    "tests/rca/test_analyzer.py",
    "tests/reports/test_generator.py",
    "tests/safety/test_redaction.py",
    "tests/services/test_reliability_artifacts.py",
    "tests/services/test_v7_live_acceptance.py"
)
git add -- $v81Paths
git commit -m "feat: complete V8.1 reliability and Provider compatibility"
$completionCommit = git rev-parse HEAD
```

Stage only files belonging to the V8.1 implementation; do not include unrelated worktree changes. If git actions are not authorized, skip this step, leave `current.md` in `verifying`, and report that the iteration cannot yet satisfy its completion-state contract.

- [ ] **Step 10: Record completion state**

After an authorized completion checkpoint exists, update `docs/superpowers/current.md`:

```text
Iteration status: complete
Implementation status: complete
Completion commit: <authorized V8.1 completion checkpoint SHA>
Verification evidence: ruff, full pytest, frontend build, git diff check, safety scan, artifact scan, and Provider certification outcomes with run IDs
Blocker: none
```

Commit this status-only update only when git actions are authorized. The completion commit field identifies the verified implementation checkpoint, not the later status-record commit.

Before the status-only commit, run `git diff --check -- docs/superpowers/current.md`. No implementation test rerun is required for this status-only metadata change.

---

## Plan Self-Review

### Spec coverage

- Mock isolation, bounded/scoped Provider results, duplicate IDs, failed/skipped support rejection, and semantic cause/reference validation: Baseline Task A.
- Redaction before persistence/API/UI, safe exceptions, all-field Markdown escaping, and Evidence ID/status rendering: Baseline Task B.
- Action/verification transition matrices, approval notes, result references, stable HTTP errors, targeted writes, and concurrency: Baseline Task C.
- Real v3/v4-to-v5 migration, schema validation, foreign keys, non-destructive aggregate saves, in-memory pooling, and container lifecycle: Baseline Task D.
- V6 ReAct/pseudo-LLM retirement, historical read compatibility, summary listing, independent frontend detail loading, and deterministic optional-runtime fallback: Baseline Task E.
- V7 artifact evidence, stop gates, classification, diagnostic cohort, and canonical gate: Tasks 4-5.
- Hybrid agreement semantics: Task 2.
- Structured execution/failure/stabilization contracts: Tasks 1-3.
- Successful, failed, and skipped Provider/model attribution across runtime, persistence, API, and frontend: Tasks 2-3 and 8.
- Atomic V7 persistence, memory/SQLite/API/frontend attribution, and old-record defaults: Task 3.
- Schema-v3 deterministic/live artifacts and latest-result certification lookup: Task 4.
- OpenAI/DeepSeek explicit Provider selection, no fallback, no mixing: Tasks 6-8.
- DeepSeek serialized JSON Object request, local validation, capability, and trace safety: Tasks 6-7.
- Fault injection, semantic-reference rejection, persistence rollback, executed-action claim checks, safety wording, and secret scans: Tasks 4, 5, and 9.
- Independent paid Provider gates: Task 10.
- Full milestone reviews and `current.md` status transitions: execution rules, milestone gates, Tasks 9-10.

### Deliberate simplifications

1. Reuse the existing `v7_live_acceptance` cohort runner instead of adding a second evaluation framework.
2. Add one explicit schema-v5 migration manifest for the real v3/v4 history; create no table outside the approved current schema and add no generic migration framework.
3. Support only explicit OpenAI and DeepSeek Providers; add no arbitrary Base URL, registry, routing, fallback, or mixed-provider execution.
4. Read certification from ignored schema-v3 artifacts; add no analytics service or certification database.

### Required execution stop points

1. Stop before paid diagnostics if Baseline Tasks A-E fail truthfulness, redaction, transition, migration, concurrency, lifecycle, or legacy-read checks.
2. Stop on a historical SQLite version/shape not covered by the approved v3/v4 compatibility contract; do not guess a repair.
3. Stop after Task 5 diagnostic if any failure is `unknown` or requires an unapproved behavior change.
4. Stop before DeepSeek paid use if official capability requirements are not met.
5. Stop and report rather than weaken any reliability or safety threshold.
